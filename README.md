# Architector — Visual Logic Node Editor

**Architector** — это интерактивный визуальный редактор иерархических графов (нодовых диаграмм), предназначенный для проектирования логики приложений, диаграмм потоков данных и архитектурных схем (например, цепочек промптов и интеграции ИИ-ассистентов).

Ключевая парадигма (v14): **Дорожки, Окна и Рамки**. Внутренность каждого узла (или корня проекта) — отдельная **дорожка** (не хранится, производная от `parentId`); **окно** — набор дорожек, положенных рядом, свободно перемещаемое на общем бесконечном мировом холсте; **рамка** — множество узлов из любых дорожек, рисуемое кусками, замена прежних «слоёв». Единственная структура родства — `parentId ∈ {'root', nodeId}`; глубина узла вычисляется длиной цепочки `parentId` до корня, а координаты каждого узла локальны дорожке его непосредственного родителя. Подробная спецификация поведения — `docs/LANES_MODEL.md`.

## 🚀 Технологический стек
Проект выполнен по схеме "zero-build" и работает полностью в браузере без необходимости предварительной сборки:
* **Core:** React 18 & ReactDOM 18 (загружаются через CDN).
* **Компиляция JSX:** Babel Standalone (транспиляция кода на лету в браузере).
* **Стилизация:** Tailwind CSS (Play CDN с поддержкой кастомных стилей и директив `@layer`).
* **Иконки:** Lucide Icons (статический шрифт).
* **Сервер:** Локальный веб-сервер на Python (`server.py`, с заголовками no-cache — иначе браузер кэширует text/babel-скрипты и после правок исполняет смесь старых и новых файлов). Дополнительно все скрипты в `index.html` подключаются с меткой версии (`?v=...`): после правок компонентов метку следует поднимать, чтобы гарантированно сбросить кэш и у пользователей, открывающих страницу без dev-сервера.

---

## 📂 Структура проекта

* `app/index.html` — главная точка входа. Подключает стили Tailwind, Google Fonts (11 гарнитур), библиотеки и скрипты компонентов типа `type="text/babel"`. Новые компоненты добавляются сюда отдельной строкой script (порядок: utils → store → компоненты → app).
* `app/app.js` — корневой компонент приложения с предохранителем `ErrorBoundary` и рендером в DOM.
* `app/run.bat` — скрипт автозапуска. Запускает `server.py` на порту 3000 и открывает страницу приложения в браузере по умолчанию.
* `app/server.py` — сервер разработки с запретом кэширования.
* `app/store/reducer.js` — чистая логика состояния: defaultState, редьюсер, миграции формата (v9 → v10 → v11 → v12 → v13), нормализация окон уровней, загрузка из localStorage. Без JSX, исполняется и в Node (тесты). ВАЖНО: редьюсер не должен вызывать сам себя — общая логика выносится в top-level функции (например, `applyRemoveLevelWindow`), иначе babel-standalone перестаёт публиковать `const reducer` для остальных скриптов.
* `app/store/Store.js` — React-провайдер поверх редьюсера (`useReducer` + `Context API`). Поддерживает:
  * Сохранение состояния в `localStorage` (ключ `architector_state_v12`; legacy-ключи `architector_state_v11` / `architector_state_v10` / `architector_state_v9` конвертируются при первом запуске, см. `docs/MIGRATIONS.md`), дебаунс 400 мс + синхронный флаш при закрытии вкладки.
  * Историю действий (Undo/Redo на 20 шагов).
  * Операции с буфером обмена (Copy/Paste узлов).
  * Массовые операции (`MASS_UPDATE`, `DELETE_SELECTED`).
* `app/utils/hierarchy.js` — иерархия и координатное ядро уровней:
  * `getLevel` / `getEntityLevel` — уровень сущности по цепочке `parentId` (правило семантического шага: слой — проходной, узел — шаг +1, id окна уровня — терминальный случай с `levelIndex` напрямую). Ещё не мигрированные v11-сущности (`ownerId`/`ownerGap`) читаются тем же путём через единый `structuralParentOf`.
  * `getLocalPosition`, `getWorldTransform`, `getPortWorldPosition`, `getNodeWorldBounds` — переводы координат «локальные ↔ мировые» с учётом камер окон.
  * `getWindowOfLevel`, `getLevelView` (камера окна: `innerOffset`, `innerZoom`, `isCollapsed` в `state.levelViews`), `LEVEL_WINDOW_METRICS`.
  * `getProxyPortsForWindow` — прокси-порты межуровневых связей на рамках окон (авторасстановка + ручные позиции `link.proxyOverrides`).
  * Изоляция веток («глаз»): `toFocusList`, `getBranchOwner`, `hasAncestorIn`, `isEntityVisible` — единая точка истины видимости (глобальный глаз Главного холста приоритетен и просвечивает ветки выделенных корней на всех уровнях; локальные глаза действуют в пределах своего уровня).
  * `getAddContext` — контекст создания узла/слоя кнопкой «+» (брат выделенного по структурному родителю; сирота-якорь на кликнутом уровне, если фокусной ветки нет; `ok:false` при массовом выделении или неоднозначной ветке).
  * `canReparentTo` — вердикт переноса сущности в контейнер (self/цикл/существование цели, единая проверка для узлов, слоёв и окон-якорей) для UI-подсказок и валидации.
  * Drag & Drop: `getDropTarget` — резолвер цели жеста (узлы/слои по пересечению контуров, окна по указателю, приоритет узел → слой → окно, потомки исключаются, свёрнутые окна не принимают дроп, dnd-off при выключенном тумблере); `getLayerWorldBounds` / `getEntityWorldBounds`; `computeDropPositions` — мировые координаты → локаль целевого окна с учётом камеры; `buildTransferConfirmText` — текст вопроса-подтверждения перед переносом.
* `app/utils/geometry.js` — математический модуль для расчета геометрии графа:
  * `getPortRelativePosition` — расчет точных координат портов на гранях узлов.
  * `buildLinkPath` — построение путей связей (Bezier / Orthogonal), единое для внутриуровневых и межуровневых сегментов.
  * `getSmartPlacement` — алгоритм автоматической раскладки узлов внутри слоя без перекрытий с динамической оптимизацией размера слоя под его контент (fit-to-content).
  * `resolveLayerCollision` — алгоритм разрешения коллизий (выталкивания) слоёв при перетаскивании, предотвращающий их наложение друг на друга. Применяется только при выключенном `ui.dragDropMode`; при включённом режиме выталкивание безусловно отключено, чтобы слои можно было завести друг в друга и вложить.
* `app/components/` — React-компоненты интерфейса:
  * `Canvas.js` — общий (мировой) холст: панорамирование, зум колесом (Ctrl/Cmd+колесо — зум мирового холста из любой точки, в том числе над окнами уровней), рендер окон уровней, глобальный SVG-слой межуровневых пунктирных связей (скрытые изоляцией концы не рисуются), хоткеи (Delete удаляет и выделенное окно уровня, включая Главный холст при наличии Уровня 1).
  * `LevelWindow.js` — пространственное окно уровня: шапка с drag-перемещением, унифицированный бейдж `[ 📁 N ]`, мастер-порт, кнопки «глаз»/свернуть/крестик (крестик удаляет уровень со сдвигом нижних вверх, а для единственного окна проекта — подтверждает удаление проекта), внутренний вьюпорт с собственной камерой (колесо — зум окна; подписка через callback-ref, переживающий сворачивание/разворачивание), прокси-порты межуровневых связей на рамке (Shift+драг двигает их по периметру), фильтрация содержимого через `isEntityVisible`.
  * `Node.js` — визуальное представление узла: авторесайз, панель действий, бейдж `📁 N` для вложенных узлов (клик — показать детей на следующем уровне); клик по узлу любого проекта мгновенно делает проект активным. Кнопка «+» в шапке убрана (PLAN_LAYERS_AND_CONTEXT_CREATION.md, разд.4): создание дочернего узла/слоя/ассистента централизовано в FAB `Toolbar.js` — при выделении узла.
  * `Layer.js` — контейнер-слой. Позволяет визуально группировать узлы и перемещать их совместно. Предотвращает перекрытие с соседними слоями за счет скольжения по границам при перетаскивании — но только при выключенном `ui.dragDropMode`; при включённом режиме скольжение безусловно отключено (не зависит от того, есть ли под курсором валидная цель), чтобы слой можно было завести внутрь другого слоя и вложить (`REPARENT_ENTITY`, тот же принцип, что и в поповере «Назначить на слой»). **Полноправный участник графа связей наравне с узлом**: несёт порты на всех 4 гранях (клик по грани в режиме `add-port`, Shift+драг скользит порт по периметру), может соединяться связями со слоями и узлами (Слой ↔ Слой, Слой ↔ Узел). Клик по слою переключает активный проект.
  * `Port.js` — точка подключения связей на границе узла ИЛИ слоя (хост-сущность разрешается полиморфно: `nodes[id] || layers[id]`) (Shift+драг — скольжение порта по периметру; перетаскивание от порта — создание связи, при сбросе в окно другого уровня узел/слой-приёмник наследует фокус-владельца ветки этого уровня). Клик по порту активирует проект.
  * `Link.js` — соединительная линия между портами узлов и/или слоёв (кривые Безье и ортогональные линии, подсветка сети). Клик по связи активирует проект.
  * `NodePreview.js` — семантическое превью геометрии и контента ноды при изменении масштаба.
  * `ContextActionBar.js` — контекстная панель свойств в едином визуальном стиле для всех типов (проект, уровень, узел, слой, порт, связь, массовое выделение): позиция `top-4`, ширина 794px, тёмный фон; бейдж типа фиксированной ширины 110px, окрашиваемый в цвет элемента (по умолчанию `#191c23`); во всех цветовых поповерах — пресеты, «Свой цвет» и пипетка (EyeDropper API, Chrome/Edge); у уровня — кнопки «Очистить» (все уровни; удаляются только сущности самого уровня, потомки выживают: «внук — деду» напрямую по `parentId` или сирота-якорь) и «Удалить» (для L≥1 — уровни ниже поднимаются; для L0 — кнопка «Удалить холст», `REMOVE_ROOT_CANVAS`: Уровень 1 становится Главным холстом с прежним именем и цветом; неактивна без других уровней, подтверждения нет — есть Undo). Кнопка «Очистить проект» в панели проекта — полный сброс содержимого всех уровней (`CLEAR_PROJECT`). Поповер «Назначить на слой» показывает слои всех уровней: чужой уровень помечен бейджем `L№` и работает через `REPARENT_ENTITY` (уровень сущности просто становится уровнем слоя); цель внутри собственной ветки отклоняется целиком (`canReparentTo`, reason: 'cycle') — отдельного «спуска» v11 больше нет. Поле «Группа» удалено из UI (легаси).
  * `Toolbar.js` — плавающая кнопка быстрого добавления (FAB справа), **контекстная в 3 режимах** (`fabContext`, PLAN_LAYERS_AND_CONTEXT_CREATION.md разд.5) в зависимости от текущего единичного выделения:
    * **`none` (по умолчанию, ничего/группа не выделены):** клик по «+» создаёт пустой узел на активном уровне (сирота-якорь без фокусной ветки); наведение курсора (Hover) или long-press (~350 мс на сенсорных экранах) раскрывает веер из пяти действий («Порт», «Слой», «Уровень», «Ассистент», «Проект»). Кнопки гаснут с подсказкой при неоднозначной цели.
    * **`node` (выделен один узел):** «+» и бейдж `L{N}` на нём создают дочерний узел на подуровне узла (то же действие, что раньше давала убранная кнопка «+» в шапке `Node.js`); веер сужается до дочернего слоя и дочернего ассистента на том же подуровне — «Порт»/«Уровень»/«Проект» притушены как нерелевантные этому выделению.
    * **`layer` (выделен один слой):** «+» и бейдж-иконка слоя на нём кладут узел ВНУТРЬ выделенного слоя (группировка через `parentId`, без смены владения); веер сужается до слоя и ассистента внутрь того же слоя — «Порт»/«Уровень»/«Проект» так же притушены.
  * `Library.js` + `OutlinerTree.js` — панель-обозреватель проекта шириной 350px (вкладки: Объекты и История); вкладка «Узлы» группирует узлы по СЛОЯМ (заголовок-слой выделяет все его узлы), дерево принимает сброс узла на слой другого уровня как перенос (`REPARENT_ENTITY`).
  * `AIAgentNodeContent.js` — интерфейс встроенного ИИ-ассистента для узлов типа `ai-agent` с возможностью автоматического изменения графа на основе JSON-команд.

* `app/types/` (`typedefs.js`, `globals.d.ts`) + `app/jsconfig.json` — JSDoc-типы и глобальные декларации TypeScript; проверка: `npx -y -p typescript tsc -p app/jsconfig.json`.
* `app/tests/` — юнит-тесты чистой логики; запуск: `node --test app/tests/*.test.js`. Точное число тестов намеренно не дублируется в документации: единственный источник правды — вывод самого прогона.
* `app/bench/` — измерительная оснастка: `core.bench.js` (чистая логика на сценах S/M/L), `render.bench.js` (перерисовки и FPS в headless-браузере), `syntax-check.js` (разбор всех JSX-файлов до открытия страницы), `offline-harness.js` (стенд с локальными копиями CDN-библиотек). Результаты — в `docs/BENCHMARKS.md`.

---

## 🛠️ Запуск проекта

1. Убедитесь, что у вас установлен Python (версии 3.x).
2. Запустите файл `run.bat` в папке `app` проекта.
3. Откроется окно браузера по адресу `http://localhost:3000`.
4. Для остановки сервера закройте свернутое консольное окно с именем `Architector Web Server`.

---

## 🗂️ Мультипроектность (v12)

Состояние верхнего уровня — набор проектов: `{ projects, projectOrder, activeProjectId, projectCounter }`. Каждый проект хранит собственные сущности, окна уровней, камеры и **собственную историю Undo**; камера общего холста, настройки интерфейса и выделение — глобальные. Панели (`Toolbar.js`, `Library.js`, `ContextActionBar.js`) и узел ИИ-ассистента (`AIAgentNodeContent.js`) по-прежнему читают плоский вид АКТИВНОГО проекта (`mergeActiveView` через `useStore()`/`useSelector()`) — для них ничего не изменилось.

Окна всех проектов живут на одном мировом холсте колонками. **С PLAN V6 неактивные проекты полностью интерактивны наравне с активным**: узлы, слои, порты и связи чужого (неактивного) проекта кликабельны, выделяемы, перетаскиваемы и редактируются на месте — компоненты холста (`Node.js`, `Port.js`, `Layer.js`, `Link.js`) читают и пишут СВОЙ проект напрямую через `ProjectContext`/`getProjectFlatView`, минуя активный проект (см. раздел «Как компоненты получают состояние» ниже). Клик по элементу неактивного проекта мягко делает его активным для верхних панелей (свойства, Undo-тулбар), не прерывая жест. Межпроектные связи между портами разных проектов реализованы (Фаза 6.1 `PLAN_V12_CLEAN_HIERARCHY_AND_INTERACTIONS.md`) — тем же жестом, что и обычная связь, визуально как межуровневая, но с пульсацией; хранятся отдельно от проектов, в `state.crossProjectLinks`. Перетаскивание узлов и целых веток между проектами тоже реализовано (Фаза 6.3) — тем же Drag&Drop-жестом, что и внутри проекта: неактивные проекты и так на общем холсте в единых мировых координатах, резолвер цели просто научился их видеть. Ключ хранилища — `architector_state_v12`; подробности перехода — в `docs/MIGRATIONS.md`.

**Проекты и уровни можно выделять пачками** (Shift+клик по плашке проекта или шапке окна), изолировать на холсте, оформлять, выравнивать и удалять — см. `docs/UX.md`.

> **История Undo не переживает перезагрузку страницы.** В пределах вкладки она работает полностью, но в хранилище снимки не пишутся: 20 снимков сущностей на проект переполняли квоту браузера на крупных проектах.

---

## ⚙️ Как компоненты получают состояние

Состояние живёт вне React — в движке `app/store/engine.js`; компоненты подписываются на него через `useSyncExternalStore`. Хук `useStore()` отдаёт полный плоский вид АКТИВНОГО проекта (прежний контракт, `mergeActiveView`), `useSelector(selector)` — точечную подписку на него же.

**Компоненты холста (Node/Port/Layer/Link) на это не полагаются.** У них есть свой проектный слой поверх того же движка (`Store.js`):
* `ProjectContext` — React-контекст, несущий `projectId` конкретного окна проекта на холсте (`null` = активный проект по умолчанию). Каждый `LevelWindow` и Drag-оверлей на холсте обёрнут в `<ProjectContext.Provider value={projectId}>` (`Canvas.js`).
* `getProjectFlatView(projectId)` — гранулярный кэш плоского вида ЛЮБОГО проекта (не только активного): инвалидируется по ссылке на `state.projects[projectId]`, а не по общему корню состояния, поэтому движение мыши и экшены в чужих проектах не сбрасывают его и не вызывают лишних перерисовок.
* `useProjectSelector(selector)` / `useProjectDispatch()` — читают `projectId` из `ProjectContext` и работают со своим проектом: чтение идёт через `getProjectFlatView(projectId)`, а `dispatch` для неактивного проекта автоматически заворачивает экшен в `{ type: 'FOR_PROJECT', payload: { projectId, action } }`, не трогая активный проект.

Панели (`Toolbar.js`, `Library.js`, `ContextActionBar.js`) используют обычные `useStore()`/`useSelector()`/`useDispatch()` и работают с АКТИВНЫМ проектом — осознанное решение для верхних панелей. Узел ИИ-ассистента (`AIAgentNodeContent.js`) — отдельный случай: он рендерится внутри `NodeView` конкретного проекта на холсте, поэтому читает и пишет `useProjectStore()` (2026-08-29) — аналог `useStore()`, но привязанный к `ProjectContext`, как у `Node.js`/`Port.js`/`Layer.js`/`Link.js`. До этой правки узел-ассистент в неактивном проекте молча читал и менял активный проект — баг того же семейства, что чинили для окон уровней.

Важное правило для новых компонентов: селектор считает производные значения (мировая позиция, уровень, видимость, флаги подсветки) и сравнивается по РЕЗУЛЬТАТУ, а не подписывается на «свою запись» в состоянии. Иначе теряется «рябь» — изменение далёкого предка, которое обязано дойти до потомка. Инвариант закреплён тестами `app/tests/reactivity.test.js`.

---

## 📝 Спецификация формата сохранений (JSON, v11)

> ⚠️ **Это формат ЛОКАЛЬНОГО файлового импорта/экспорта одного проекта** (`LOAD_STATE`, кнопка «Импорт проекта» / «Экспорт проекта») — исторический пример ниже по-прежнему честно описывает старые поля `ownerId`/`ownerGap`/`homeLevel` (файлы с ними продолжают читаться корректно). Живая модель ВНУТРИ приложения — parentId-only (см. «Модель данных v13» в `AGENTS.md` и `docs/IDEAL_INTERACTIONS.md`): все новые сущности создаются и переносятся уже без `ownerId`, а с Фазы 6.5 сам импорт (`ADD_PROJECT_FROM_FILE`/`MERGE_PROJECT_FROM_FILE`) дополнительно прогоняет загруженный файл через `migrateProjectEntitiesToV13` — так что и старые, и свежие файлы одинаково приводятся к чистому `parentId`, независимо от того, из какой версии реально пришли. Локальный экспорт (Фаза 6.2) может нести дополнительное поле `externalGateways` — записи о кросс-проектных связях, обрывающихся на границе экспортируемого проекта, воспроизводимые при повторном импорте обеих половин. Отдельно от локального формата — **глобальный** формат целого рабочего пространства (`kind: "global"`, Фаза 6.4): все проекты разом плюс `crossProjectLinks`, экспортируется/импортируется кнопками «Экспортировать всё»/«Импортировать всё» — см. AGENTS.md, «Глобальный экспорт/импорт рабочего пространства».

Проекты Architector экспортируются и импортируются в виде JSON-файлов со следующей структурой:

```json
{
  "formatVersion": 11,
  "levelWindows": {
    "lvlwin-root": {
      "id": "lvlwin-root",
      "levelIndex": 0,
      "name": "Главный холст",
      "content": "",
      "color": "#1e293b",
      "position": { "x": -500, "y": -400 },
      "size": { "w": 1000, "h": 700 },
      "fontFamily": "Inter, sans-serif",
      "fontSize": 14
    }
  },
  "levelViews": {
    "lvlwin-root": {
      "innerOffset": { "x": 0, "y": 0 },
      "innerZoom": 1,
      "isCollapsed": false
    }
  },
  "layers": {
    "layer-id-1": {
      "id": "layer-id-1",
      "name": "Название слоя",
      "content": "Описание назначения слоя",
      "color": "#1d4ed8",
      "position": { "x": 60, "y": 200 },
      "size": { "w": 900, "h": 950 },
      "locked": false,
      "parentId": "root",
      "ownerId": null,
      "snapToGrid": true
    }
  },
  "nodes": {
    "node-id-1": {
      "id": "node-id-1",
      "name": "Имя узла",
      "content": "Текст или логика внутри узла",
      "color": "#1e293b",
      "position": { "x": 70, "y": 100 },
      "size": { "w": 400, "h": 240 },
      "parentId": "root",
      "ownerId": "node-parent-id",
      "snapToGrid": true,
      "shape": "rectangle",
      "type": "default"
    }
  },
  "ports": {
    "port-id-1": {
      "id": "port-id-1",
      "nodeId": "node-id-1",
      "type": "output",
      "edge": "right",
      "position": 0.5,
      "name": "Имя порта",
      "color": "#3b82f6"
    }
  },
  "links": {
    "link-id-1": {
      "id": "link-id-1",
      "sourcePortId": "port-id-1",
      "targetPortId": "port-id-2",
      "name": "Название связи",
      "content": "Описание передаваемых данных",
      "color": "#3b82f6",
      "linkStyle": "bezier",
      "proxyOverrides": {
        "lvlwin-root": { "edge": "right", "fraction": 0.5 }
      }
    }
  },
  "activeLevelIndex": 0,
  "levelFocusParentId": { "1": ["node-parent-id"] },
  "levelHideNeighbors": { "1": true },
  "canvas": {
    "offset": { "x": 100, "y": 200 },
    "zoom": 0.65
  },
  "isolatedIds": []
}
```

### Детали полей:
* **Окна уровней (`levelWindows`):** словарь по стабильному `id` окна (окно Главного холста — `lvlwin-root`). Номер уровня — поле `levelIndex`; при удалении уровня нижележащие окна сохраняют id и рамку (имя, цвет, позицию, размер), меняется только их `levelIndex`.
* **Камеры окон (`levelViews`):** панорама (`innerOffset`), зум (`innerZoom`) и свёрнутость (`isCollapsed`) каждого окна; ключ — id окна. Живут вне снапшотов истории: Undo структурных правок не дёргает точку обзора.
* **Система координат (v11):** `position` узла/слоя локальна холсту его уровня. `parentId` — координатный контейнер («где лежит»): `"root"` = холст уровня, либо id слоя-контейнера. Родство между уровнями выражается `ownerId`: уровень сущности равен длине цепочки владения (`HierarchyUtils.getEntityLevel`).
* **Версия формата (`formatVersion`):** текущая — `11`. Файлы v10 (относительные координаты от родителя) и v9 (мировые координаты) конвертируются при импорте автоматически. Детали: `docs/MIGRATIONS.md`.
* **Изоляция веток («глаз»):** `levelHideNeighbors` — включённость глаза по уровням (ключ — номер уровня; уровень 0 — глобальный глаз, приоритетный над локальными); `levelFocusParentId` — фокус-наборы веток по уровням (массивы id владельцев; легаси-строка читается как набор из одного).
* **Сироты-якоря (`homeLevel`):** необязательное поле узлов и слоёв; сущность без `ownerId` с `homeLevel: N` живёт на уровне N как глава независимой ветки (её дети — на N+1). Поля нет — уровень 0 (прежнее поведение). Поле `group` узлов — легаси, интерфейсом не используется.
* **Связь через поколение (`ownerGap`):** необязательное поле узлов и слоёв; дистанция до владельца в уровнях (нет поля — 1, обычное родство). Появляется при очистке промежуточного уровня: выжившие потомки пере-якорятся на ближайшего живого предка («внук — деду») и остаются на своих уровнях. `getLevel` суммирует дистанции по цепочке; перенос через `REPARENT_ENTITY` сбрасывает поле целиком (сущность становится parentId-only, v13); удаление уровня, который связь перепрыгивала, сокращает её на 1. Старые проекты читаются без миграции.
* **Ручные позиции прокси-портов (`links[].proxyOverrides`):** словарь `{ [windowId]: { edge, fraction } }` — положение прокси-порта межуровневой связи на рамке окна (задаётся Shift+драгом). Прокси без записи авторасставляются по граням.
* **Мастер-порты уровней:** порты с id `port-master-level-N` (`isMaster: true`, `windowIndex: N`) принадлежат окнам уровней, а не узлам; при сдвиге уровней перенумеровываются вместе со ссылками из связей.
* **Грани портов (`edge`):** `left`, `right`, `top`, `bottom`; `position` — смещение вдоль грани (0.0–1.0).
* **Типы узлов (`type`):** `'default'` или `'ai-agent'` (интерактивная панель чата с ИИ-ассистентом).
* **Изоляция с тулбара (`isolatedIds`):** массив ID элементов, оставленных видимыми в режиме изоляции. Пустой массив — изоляция выключена.
* **Режим Drag&Drop (`ui.dragDropMode`):** тумблер в панели свойств проекта. Включён — жестом доступны межуровневые переносы и вложения (дроп на узел = стать его ребёнком; дроп в слой = положить в слой с авторасстановкой; дроп на холст чужого окна = сирота на том уровне, элементы ложатся под курсором) — каждый перенос предваряется вопросом-подтверждением, весь жест откатывается одним Undo; выталкивание слоёв друг от друга (`resolveLayerCollision`) на время жеста безусловно отключено, чтобы можно было вложить один слой в другой. Выключен (дефолт при каждой загрузке) — элементы не пересекают границы своих окон (клампинг), доступна только группировка в слои своего уровня; кросс-уровневые строки поповеров тусклые с подсказкой. Транзиентный стейт жеста — `state.dragGesture` (вне истории и сохранений): пока идёт жест, перетаскиваемая сущность и её parentId-вложенные потомки (например, узел, назначенный на переносимый слой) рисуются отдельным оверлеем поверх всех окон (`Canvas.js`), не обрезаемым границей исходного окна, — иначе вложенный потомок визуально пропадал бы в момент пересечения границы окна.
