# Инструкции для ИИ-ассистентов (AGENTS.md)

Данный документ содержит ключевые архитектурные правила, спецификацию структуры и стандарты разработки для приложения **Architector**. Все ИИ-модели, работающие с этой кодовой базой, должны строго соблюдать изложенные здесь соглашения.

---

## 🏛️ Архитектурная философия (Zero-Build)

1. **Отсутствие бандлера и сборки (Zero-Build):**
   * Приложение работает напрямую в браузере без Webpack, Vite, Rollup или Babel CLI.
   * Все глобальные библиотеки (React 18, ReactDOM, Babel Standalone, Tailwind CSS Play CDN, Lucide Icons) подключаются через CDN в `app/index.html`.
   * Компоненты подключаются тегами `<script type="text/babel" src="...">` в `app/index.html`.
   * **КРИТИЧЕСКОЕ ПРАВИЛО:** В файлах компонентов **НЕЛЬЗЯ использовать синтаксис ES-модулей `import` / `export`** для браузерного скриптинга. Все компоненты и утилиты объявляются в общем глобальном скоупе Babel.

2. **Двойной экспорт утилит (Browser + Node.js):**
   * Файлы `app/utils/geometry.js`, `app/utils/hierarchy.js` и `app/store/reducer.js` исполняются как в браузере, так и в средах без DOM (в тестовом раннере `node:test`).
   * В конце каждого такого файла используется схема двойного экспорта:
     ```js
     if (typeof module !== 'undefined' && module.exports) {
         module.exports = GeometryUtils;
     } else {
         window.GeometryUtils = GeometryUtils;
     }
     ```

---

## 🗺️ Карта проекта и назначение файлов

```
architector-main/
├── app/
│   ├── index.html                 # Главная точка входа, подключение всех скриптов и CDN
│   ├── app.js                     # Корневой компонент с ErrorBoundary и вызовом ReactDOM.render
│   ├── server.py                  # Python dev-сервер с заголовками no-cache
│   ├── run.bat                    # Скрипт автозапуска сервера и открытия браузера
│   ├── store/
│   │   ├── reducer.js             # Чистая логика состояния, initialState, миграции (без JSX)
│   │   ├── engine.js              # Движок стора без React: подписки, кэш селекторов, профилировщик рендеров
│   │   └── Store.js               # React Context Provider + localStorage + fallback памяти
│   ├── utils/
│   │   ├── hierarchy.js           # Уровни (ownerId), координатное ядро окон, камеры (levelViews),
│   │   │                          #   прокси-порты, видимость (isEntityVisible), getAddContext
│   │   └── geometry.js            # Геометрия портов, построение путей связей, авторасстановка, коллизии
│   ├── components/
│   │   ├── Canvas.js              # Мировой холст, жесты, Ctrl/Cmd+колесо = мировой зум, хоткеи,
│   │   │                          #   глобальный слой межуровневых связей
│   │   ├── LevelWindow.js         # Пространственное окно уровня: шапка, вьюпорт с камерой,
│   │   │                          #   глаз-изоляция, прокси-порты на рамке (Shift+драг)
│   │   ├── Node.js                # Отрисовка ноды, перетаскивание, автоподстройка размера
│   │   ├── Layer.js               # Отрисовка слоя, группировка нод, предотвращение перекрытий
│   │   ├── Port.js                # Точки привязки связей на гранях нод
│   │   ├── Link.js                # Отрисовка связей (Bezier и Orthogonal)
│   │   ├── NodePreview.js         # Компонент превью контента ноды при семантическом масштабировании
│   │   ├── ContextActionBar.js    # Верхняя контекстная панель свойств (Узлы, Слои, Порты, Связи, Массовое)
│   │   ├── Toolbar.js             # Левая панель инструментов (добавление, выравнивание, экспорт/импорт, изоляция)
│   │   ├── Library.js             # Левый обозреватель проекта (вкладки: Объекты, История; дерево и иерархия)
│   │   ├── OutlinerTree.js        # Иерархическое дерево узлов и слоев (Drag & Drop перевложение)
│   │   └── AIAgentNodeContent.js  # Виджет ИИ-ассистента для нод типа 'ai-agent'
│   ├── bench/
│   │   ├── core.bench.js          # Замер чистой логики на сценах S/M/L
│   │   ├── render.bench.js        # Замер перерисовок и FPS в headless-браузере
│   │   ├── syntax-check.js        # Шлюз: разбор всех JSX-файлов Babel до открытия страницы
│   │   └── offline-harness.js     # Стенд с локальными копиями CDN-библиотек
│   ├── types/
│   │   ├── typedefs.js            # Центральные JSDoc-типы (NodeEntity, LayerEntity, LinkEntity)
│   │   └── globals.d.ts           # Глобальные TypeScript-декларации для window/module утилит и компонентов
│   └── tests/
│       ├── reducer.test.js        # Юнит-тесты редьюсера: Undo/Redo, уровни (удаление/очистка/сдвиг),
│       │                          #   изоляция веток, getAddContext, прокси-порты, Drag&Drop
│       ├── coords.test.js         # Юнит-тесты координатного ядра уровней
│       ├── geometry.test.js       # Юнит-тесты авторасстановки и выталкивания
│       ├── hierarchy.test.js      # Юнит-тесты иерархии и защиты от зацикливания
│       ├── migration.test.js      # Юнит-тесты миграций v9 → v10 → v11
│       ├── persistence.test.js    # Персистентность сессий ИИ, безопасность, пакет истории, кэш и индексы
│       ├── engine.test.js         # Движок стора: подписки, отсечение пустых dispatch, кэш селекторов
│       ├── reactivity.test.js     # «Рябь»: изменение предка обязано доходить до потомка
│       ├── selection.test.js      # Выделение контейнеров, изоляция, массовое удаление
│       └── fixtures/generate.js   # Детерминированный генератор сцен для бенчмарков и тестов
└── docs/
    ├── ARCHITECTURE.md            # Детальная архитектурная спецификация и инварианты
    ├── CHANGELOG.md               # История изменений и рефакторингов
    ├── MIGRATIONS.md              # Документация конвертации форматов данных
    ├── PLAN.md                    # План развития и реализованные этапы
    └── UX.md                      # Спецификация хоткеев, жестов и пользовательского опыта
```

---

## 📐 Модель данных v11 и Инварианты

1. **Формат сохранения (`formatVersion: 11`) — пространственные уровни:**
   * Каждый уровень иерархии — отдельное окно (`levelWindows`, ключ — стабильный id окна, номер уровня — поле `levelIndex`; окно L0 — `lvlwin-root`). Камеры окон живут в `levelViews` (вне истории Undo).
   * **Родство ≠ контейнер.** Родство между уровнями выражается полем `ownerId` (владелец на уровне выше); уровень сущности = длина цепочки владения (`HierarchyUtils.getEntityLevel`). `parentId` — только координатный контейнер: `'root'` (холст уровня) или id слоя.
   * **Сироты-якоря (`homeLevel`).** Сущность без владельца может нести `homeLevel` — она живёт на этом уровне как глава независимой ветки, её дети — ниже. «+» без фокусной ветки создаёт такого сироту на кликнутом уровне. `TRANSFER_NODE` переносит узлы между уровнями (усыновление веткой целевого слоя; слой собственной ветки = «спуск»: узел и прямые дети становятся сиротами-братьями, поддеревья не меняются).
   * `position` локальна холсту уровня (или слою-контейнеру). Мировые координаты вычисляются через `HierarchyUtils.getWorldTransform` / `getPortWorldPosition` с учётом позиции окна и его камеры.
   * **Связь через поколение (`ownerGap`).** Обычно владелец живёт ровно на уровень выше (дистанция 1, поле отсутствует). Очистка промежуточного уровня пере-якоривает выживших потомков на ближайшего живого предка и записывает дистанцию в `ownerGap` (2, 3, …): узел на Главном холсте может не иметь ребёнка на L1, но иметь «внука» на L2. `HierarchyUtils.getLevel` суммирует дистанции по цепочке; `getOwnerGap` нормализует значение (нет поля/мусор → 1). Старые проекты читаются без миграции. Прямое назначение владельца (`TRANSFER_NODE`) дистанцию сбрасывает; «+» у брата узла с `ownerGap > 1` наследует дистанцию (`getAddContext.ownerGap`).
   * Удаление уровня N≥1 сдвигает нижние уровни вверх («следующий становится предыдущим»): потомки удалённых узлов пере-якорятся на владельца удалённого («внук — деду», дистанции складываются минус снятый уровень; связи, перепрыгивавшие удаляемый уровень, сокращают `ownerGap` на 1), окна сохраняют id/рамку/камеру, мастер-порты перенумеровываются. Главный холст (L0) удаляется ТОЛЬКО отдельным экшеном `REMOVE_ROOT_CANVAS` (кнопка «Удалить холст»): окно уровня 1 становится Главным холстом, не меняя имя и цвет; без других уровней — no-op.
   * Очистка уровня (`CLEAR_LEVEL_WINDOW`) удаляет ТОЛЬКО сущности самого уровня: потомки выживают на своих уровнях (пере-якорение «внук — деду» с ростом `ownerGap`; без живых предков — сирота-якорь `homeLevel` со своей веткой). Очистка L0 делает всех детей уровня 1 сиротами. Полный сброс содержимого всех уровней — отдельный экшен `CLEAR_PROJECT`.
   * **Режим Drag&Drop (`ui.dragDropMode`).** Межуровневые переносы и вложения ЖЕСТОМ (и кросс-уровневые пути в поповерах/дереве) доступны только при включённом тумблере (кнопка в панели проекта). Выключен (дефолт при каждой загрузке): перетаскивание клампится рамкой своего окна; всегда доступна лишь группировка в слои своего уровня. Резолвер целей — `HierarchyUtils.getDropTarget` (контуры для узлов/слоёв, указатель для окон, приоритет узел → слой → окно, потомки исключены); дроп применяет `TRANSFER_NODE` после вопроса-подтверждения (`buildTransferConfirmText`), отмена — `RESTORE_ENTITIES`. Переносимые элементы на время жеста рендерятся поверх окон (оверлей в Canvas, `state.dragGesture`).

2. **Ключи словарей:**
   * В объектах `nodes`, `layers`, `ports`, `links` ключ в словаре **ОБЯЗАТЕЛЬНО** совпадает с `entity.id` (например, `nodes["node-1"].id === "node-1"`).

3. **Форма нод (`shape`):**
   * Все узлы имеют стандартизированную прямоугольную форму `shape: "rectangle"`.

4. **Предотвращение двойного смещения:**
   * При перемещении родителя его потомки перемещаются автоматически за счет относительных координат. При массовом выделении проверяется `hasSelectedAncestor`, чтобы дочерние элементы не сдвигались дважды.

5. **Разделение явного выделения и визуальной подсветки:**
   * Обработчики событиий (`handleMouseDown`) и логика Redux проверяют `isExplicitlySelected = state.selectedIds.includes(id)`.
   * Визуальный рендеринг компонентов (`isSelected`) учитывает косвенное подсвечивание соединенных сетей связей/портов/узлов. `selectedIds` при клике хранит строго выбранную сущность для единичного редактирования свойств.

6. **Порядок скриптов в `index.html`:**
   * Скрипты подключаются strictly по цепочке: `utils/` → `store/` → `components/` → `app.js`. Новый компонент обязательно регистрируется в `app/index.html`.

7. **Визуализация портов и сетей связей:**
   * Стандартный размер порта: `w-3 h-3` (12px).
   * При выделении или подсвечивании соединенной сети порт масштабируется с коэффициентом `scale-[2.1]`.
   * **Порты и связи слоёв (2026-08-28).** Слой — полноправный участник графа связей наравне с узлом: несёт порты на всех 4 гранях (клик по грани в режиме `add-port`; Shift+драг скользит порт по периметру слоя, как у узла), соединяется связями Слой ↔ Слой и Слой ↔ Узел. Хост-сущность порта разрешается полиморфно (`state.nodes[id] || state.layers[id]`) в `getPortWorldPosition`/`getCrossLinksByLevel` (`hierarchy.js`) и в компонентах `Port.js`/`Link.js`. Удаление слоя каскадно удаляет его порты и подключённые связи. Формат хранения не менялся (`formatVersion: 11`).

8. **Управление свойствами через верхнюю контекстную панель (Context Action Bar):**
   * Все свойства узлов (цвет, значок, медиа-ссылка, перенос на слой, группа, привязка к сетке, удаление, типографика) настраиваются через единую верхнюю контекстную панель свойств.


9. **Динамический авторесайз и перенос текста (Live Auto-Sizing):**
   * При инлайн-редактировании имени (`name`) и описания (`content`) размеры узла пересчитываются мгновенно на лету (`onChange`, `skipHistory: true`).
   * Для заголовка поддерживается перенос строк (`break-all`, `nameLines`) с автоматическим увеличением высоты шапки и узла до ширины A4 (794px).
   * В обычном авторежиме полосы прокрутки отсутствуют, а кастомный скроллбар (`custom-scrollbar`) появляется только при ручном сжатии узла пользователем (`userResized: true`).

---

## 🤖 Спецификация и протокол управления для внешних ИИ-Агентов (API & Function Calling Protocol)

Данный раздел является **официальной спецификацией для подключенных внешних ИИ-агентов (LLM через OpenAI API, Anthropic Claude, Gemini или локальные нейросети)**, которые генерируют или модифицируют графы в приложении Architector.

### 1. Формат выполнения команд

Любые изменения графа со стороны ИИ-агента передаются в виде **массива Redux-экшенов в JSON-блоке** в конце текстового ответа:
```json
[
  { "type": "ACTION_NAME", "payload": { ... } }
]
```

### 2. Спецификация сущностей и типов данных

#### 🎨 Слои (`layer`)
Слой — это визуальный фрейм-контейнер для группировки узлов.
* `id` *(string, обязательный)*: уникальный ID (например `"layer-ui"`).
* `name` *(string, обязательный)*: заголовок слоя.
* `content` *(string)*: краткое описание назначения слоя.
* `color` *(string)*: HEX-цвет границы и фона шапки (например `"#0284c7"`).
* `fontFamily` *(string, опционально)*: гарнитура шрифта (например `"Montserrat, sans-serif"`).
* `fontSize` *(number, опционально)*: размер шрифта заголовка (11..24, по умолчанию 14).
* `position` *({x: number, y: number}, обязательный)*: относительно родителя.
* `size` *({w: number, h: number}, обязательный)*: размеры (рекомендуется от 500x400px).
* `parentId` *(string)*: ID родительского контейнера (по умолчанию `"root"`).
* `snapToGrid` *(boolean)*: удержание сетки (всегда `true`).

#### 📦 Узлы (`node`)
Узел — центральный компонент логики или структуры.
* `id` *(string, обязательный)*: уникальный ID (например `"node-viewport"`).
* `name` *(string, обязательный)*: название узла.
* `content` *(string)*: текстовое или маркдаун описание.
* `color` *(string)*: HEX-цвет фона шапки узла.
* `fontFamily` *(string, опционально)*: гарнитура шрифта (например `"Fira Code, monospace"`).
* `fontSize` *(number, опционально)*: кегль шрифта (11..24, по умолчанию 14, динамически масштабирует шапку и узел).
* `position` *({x: number, y: number}, обязательный)*: координаты относительны родителя.
* `size` *({w: number, h: number}, обязательный)*: габариты (автоподстройка по умолчанию, базовая ширина 220-300px).
* `parentId` *(string)*: ID родительского контейнера (`"root"`, ID слоя или ID узла).
* `shape` *(string)*: форма узла — `"rectangle"` (по умолчанию).
* `type` *(string)*: `"default"` (обычный) или `"ai-agent"` (интерактивный ИИ-копилот).
* `icon` *(string, опционально)*: иконка Lucide (`"icon-box"`, `"icon-bot"`) или эмодзи (`"🤖"`).
* `mediaUrl` *(string, опционально)*: URL картинки для медиа-карточки.
* `mediaHeight` *(number, опционально)*: высота картинки в пикселях (например `80`).

#### 🔌 Порты (`port`)
Точка привязки линий на гранях узла ИЛИ слоя — слой полноправно участвует в графе связей наравне с узлом (см. §7 в разделе «Модель данных»).
* `id` *(string, обязательный)*: уникальный ID (например `"port-vp-out"`).
* `nodeId` *(string, обязательный)*: ID сущности-владельца — узла **или слоя**. Несмотря на имя поля (легаси), значением может быть как `node.id`, так и `layer.id`.
* `type` *(string)*: `"input"` (вход) или `"output"` (выход).
* `edge` *(string)*: грань узла — `"left"`, `"right"`, `"top"`, `"bottom"`.
* `position` *(number 0.0..1.0)*: смещение вдоль грани (0.5 = середина).
* `name` *(string)*: подпись порта.
* `content` *(string, опционально)*: описание передаваемых данных порта.
* `fontFamily` *(string, опционально)*: гарнитура шрифта.
* `fontSize` *(number, опционально)*: размер шрифта (по умолчанию 12).
* `color` *(string)*: HEX-цвет точки привязки.

#### ⚡ Связи (`link`)
Линии, соединяющие порты. Так как порт может принадлежать и узлу, и слою, связь допустима в любой комбинации: Узел ↔ Узел, Слой ↔ Слой, Узел ↔ Слой.
* `id` *(string, обязательный)*: уникальный ID (например `"link-ui-store"`).
* `sourcePortId` *(string, обязательный)*: ID порта-источника.
* `targetPortId` *(string, обязательный)*: ID порта-приемника.
* `name` *(string)*: подпись связи (например `"HTTP Dispatch"`).
* `content` *(string, опционально)*: описание назначения связи.
* `linkStyle` *(string)*: `"orthogonal"` (прямые углы) или `"bezier"` (плавная кривая).
* `fontFamily` *(string, опционально)*: гарнитура шрифта.
* `fontSize` *(number, опционально)*: размер шрифта (по умолчанию 12).
* `color` *(string)*: HEX-цвет линии.
* `context` *(string)*: контекст отображения (обычно совпадает со слоем или `"root"`).

---

### 3. Математика координат (v11)

1. **Если `parentId === 'root'`:** координаты `position` локальны холсту уровня, на котором живёт сущность (уровень определяется цепочкой `ownerId`). Для уровня 0 это координаты внутри окна Главного холста.
2. **Если `parentId` — id слоя:** координаты считаются от левого верхнего угла слоя-контейнера. Безопасный внутренний диапазон для слоя 600×400: `x: 30..350`, `y: 80..280`.
3. **Родитель узла задаётся через `ownerId`** (или через `parentId` при создании — `normalizeContainer` в редьюсере сам переносит id узла-родителя из `parentId` в `ownerId`). Узел с владельцем уровня N живёт на уровне N+1 и отображается в окне этого уровня.

---

### 4. Поддерживаемые Redux-экшены (API Спецификация)

| Экшен | Payload | Описание |
|---|---|---|
| `ADD_LAYER` | `{ id, name, content, color, position, size, parentId, fontFamily?, fontSize? }` | Добавить новый слой-фрейм |
| `ADD_NODE` | `{ id, name, content, color, position, size, parentId, shape: "rectangle", type, mediaUrl, mediaHeight, icon?, fontFamily?, fontSize? }` | Добавить прямоугольный узел |
| `ADD_PORT` | `{ id, nodeId, type, edge, position, name, color, content?, fontFamily?, fontSize? }` | Добавить порт на грань узла ИЛИ слоя (`nodeId` — id владельца любого из двух типов) |
| `ADD_LINK` | `{ id, sourcePortId, targetPortId, name, linkStyle, color, context, content?, fontFamily?, fontSize? }` | Создать связь между портами |
| `CREATE_NESTED_NODE` | `{ parentId, nodeName?, nodeColor? }` | Создать вложенный узел на следующем уровне и окно при необходимости |
| `FOCUS_CHILDREN_OF_NODE` | `{ parentId }` | Сфокусировать окно следующего уровня на детях данного родителя |
| `MOVE_LEVEL_WINDOW` | `{ index, windowId?, dx?, dy?, position?, skipHistory? }` | Переместить пространственное окно уровня. **При нескольких открытых проектах передавайте `windowId`**: `index` (номер уровня) одинаков в каждом проекте, поэтому без `windowId` `multiReducer` не может определить окно-владельца и по умолчанию адресует экшен АКТИВНОМУ проекту, даже если нужное окно принадлежит другому |
| `RESIZE_LEVEL_WINDOW` | `{ index, windowId?, size: { w, h }, skipHistory? }` | Изменить габариты пространственного окна уровня. Та же оговорка про `windowId`, что и у `MOVE_LEVEL_WINDOW` |
| `ALIGN_LEVEL_WINDOWS` | - | Автоматически выстроить окна уровней вертикально по порядку L0..LN |
| `TOGGLE_LEVEL_NEIGHBORS` | `{ levelIndex, windowId?, id? }` | Включить/выключить «глаз» уровня. Уровень 0 — ГЛОБАЛЬНЫЙ глаз: просвечивает ветки выделенных корней на всех уровнях, игнорируя локальные; уровни ≥1 — локальная изоляция веток по владельцам (`levelFocusParentId[N]` — массив) |
| `SET_LEVEL_FOCUS` | `{ levelIndex, focusParentId? }` | Сделать уровень активным для создания элементов (клик в окно); фокус ветки меняется только при явной передаче |
| `TOGGLE_LEVEL_COLLAPSE` | `{ index, windowId?, id? }` | Свернуть/развернуть окно уровня до шапки. **Передавайте `windowId` / `id`** при нескольких открытых проектах — см. оговорку у `MOVE_LEVEL_WINDOW` |
| `UPDATE_PROJECT_PROPERTIES` | `{ updates: { projectName, projectColor, projectFontFamily, projectContent } }` | Обновить глобальные свойства проекта |
| `UPDATE_LEVEL_PROPERTIES` | `{ index, windowId?, updates: { name, color, fontFamily, fontSize, content, innerOffset, innerZoom } }` | Обновить свойства конкретного окна уровня (в т.ч. зум/панорамирование внутренней камеры окна — `innerZoom`/`innerOffset`). **Передавайте `windowId`** при нескольких открытых проектах — см. оговорку у `MOVE_LEVEL_WINDOW` |
| `REMOVE_LEVEL_WINDOW` | `{ index?, windowId?, id? }` | Удалить уровень N≥1: его сущности удаляются, потомки пере-якорятся «внук — деду» (дистанции `ownerGap` складываются минус снятый уровень), нижние уровни поднимаются на один с сохранением рамок и камер окон. Для уровня 0 — no-op |
| `REMOVE_ROOT_CANVAS` | — | Удалить Главный холст (кнопка «Удалить холст»): его сущности удаляются, окно уровня 1 становится Главным холстом, НЕ меняя имя/цвет/камеру, потомки поднимаются на уровень вверх. Без других уровней — no-op (кнопка неактивна) |
| `CLEAR_LEVEL_WINDOW` | `{ index?, windowId?, id? }` | Очистить уровень: удалить ТОЛЬКО его сущности. Потомки выживают на своих уровнях: пере-якорятся на ближайшего живого предка со связью через поколение (`ownerGap`), а без живых предков становятся сиротами-якорями (`homeLevel`), сохранив ветки. Окна, рамки и камеры не трогаются |
| `CLEAR_PROJECT` | — | Полная очистка содержимого ВСЕХ уровней (узлы, слои, связи, обычные порты). Окна уровней, мастер-порты, камеры и настройки проекта сохраняются |
| `UPDATE_PROXY_PORT` | `{ linkId, windowId, edge, fraction, skipHistory? }` | Ручное положение прокси-порта межуровневой связи на рамке окна (пишется в `link.proxyOverrides[windowId]`; fraction клампится в 0.03..0.97) |
| `TRANSFER_NODE` | `{ ids \| id, targetLayerId? \| targetLevelIndex?, newOwnerId?, position?, positionsById?, historySnapshot? }` | Перенос узлов в слой / на уровень: свой уровень — группировка (родство неприкосновенно), а с явным `newOwnerId` — усыновление БЕЗ смены уровня и позиции (дроп на узел); чужой — усыновление веткой слоя / `newOwnerId` (или сирота-якорь `homeLevel` без владельца); слой собственной ветки — «спуск» (узел и прямые дети — сироты-братья, поддеревья не меняются). `positionsById` — целевые локальные позиции (Drag&Drop: элементы ложатся под курсором); `historySnapshot` — срез на mousedown, чтобы весь жест был одним шагом Undo. Массово — «только верхние» |
| `SET_DRAG_GESTURE` | `{ ids, target } \| null` | Транзиентный стейт жеста Drag&Drop (`state.dragGesture`): оверлей поверх окон и подсветка целей. Вне истории и сохранений |
| `RESTORE_ENTITIES` | `{ nodes?, layers?, ports?, links? }` | Откат жеста без записи истории: возврат словарей сущностей к срезу mousedown (отмена подтверждения, Esc, бросок в пустоту) |
| `UPDATE_NODE` | `{ id, updates: { name, content, color, mediaUrl, icon, fontFamily, fontSize } }` | Обновить свойства узла |
| `UPDATE_LAYER` | `{ id, updates: { name, content, color, size, fontFamily, fontSize } }` | Обновить параметры слоя |
| `UPDATE_PORT` | `{ id, updates: { name, color, edge, position, content, fontFamily, fontSize } }` | Переместить, переименовать порт или настроить шрифт |
| `UPDATE_LINK` | `{ id, updates: { name, color, linkStyle, content, fontFamily, fontSize } }` | Изменить стиль, цвет, описание или шрифт связи |
| `REPARENT_ENTITY` | `{ id, newParentId }` | Перенести элемент в другой контейнер без сдвига в мире |
| `DELETE_SELECTED` | без payload (работает по `state.selectedIds`) | Каскадно удалить выделенные элементы с потомками. Если выделено окно уровня (`level-window-N`) — удаляет уровень (кроме L0) |
| `ALIGN_LAYERS` | `{ contextId }` | Автоматически выровнять слои в контексте |
| `CENTER_ON_ENTITY` | `id` | Сфокусировать и центрировать камеру на элементе |
| `REMOVE_NODE` | `nodeId` | Прицельно удалить конкретный узел с его портами и потомками |
| `REMOVE_LAYER` | `layerId` | Прицельно удалить слой без потери детей |
| `REMOVE_PORT` / `REMOVE_LINK` | `id` | Прицельно удалить конкретный порт или конкретную связь |
| `MASS_UPDATE` | `{ ids: string[], updates?: {}, updatesById?: {} }` | Массово обновить свойства группы элементов (цвет, шрифт, размер, сетка) |
| `CREATE_AI_SESSION` | `{ nodeId }` | Создать новую изолированную сессию диалога для узла ассистента |
| `SWITCH_AI_SESSION` | `{ nodeId, sessionId }` | Переключить активную сессию диалога узла |
| `DELETE_AI_SESSION` | `{ nodeId, sessionId }` | Удалить сохраненную сессию диалога узла |
| `ADD_AI_MESSAGE` | `{ nodeId, message: { role, content, media? } }` | Добавить сообщение в активную сессию диалога узла |
| `CLEAR_AI_HISTORY` | `{ nodeId? }` | Очистить историю сообщений узла или всех узлов |
| `UPDATE_AI_SETTINGS` | `{ provider?, model?, baseUrl?, mode?, confirmMode?, contextMode? }` | Обновить параметры ИИ-ассистента (`confirmMode: 'ask' \| 'auto'`) |

#### Мультипроектность (v12)

| Экшен | Payload | Описание |
|---|---|---|
| `ADD_PROJECT` | `{ name? }` | Создать пустой проект правее существующих и сделать его активным |
| `ADD_PROJECT_FROM_FILE` | данные проекта | Импорт файла как ДОБАВЛЕНИЕ проекта (не замена) |
| `REMOVE_PROJECT` | `{ id? }` | Удалить проект целиком. Необратимо: межпроектной истории нет |
| `SET_ACTIVE_PROJECT` | `id` | Сделать проект активным (сбрасывает выделение) |
| `FOR_PROJECT` | `{ projectId, action }` | Доставить любой внутренний экшен в КОНКРЕТНЫЙ проект, не делая его активным |

**Неактивные проекты полностью интерактивны (PLAN V6, 2026-08-28).** Узел, слой, порт и связь чужого (неактивного) проекта на общем холсте кликабельны, выделяются, редактируются инлайн и перетаскиваются наравне с активным — ограничение `pointer-events: none` снято. Клик по элементу неактивного проекта мягко (по `onClick`, не по `mousedown`, чтобы не срывать жест) синхронизирует активный проект для верхних панелей.

**Как компоненты холста получают состояние своего проекта (`ProjectContext`).** Компоненты холста (`Node.js`, `Port.js`, `Layer.js`, `Link.js`) НЕ читают активный проект напрямую — у них есть проектно-адресный слой поверх `Store.js`:
* `ProjectContext` — React-контекст с `projectId` того окна на холсте, в котором рендерится компонент (`Canvas.js` оборачивает каждое окно и Drag-оверлей в `<ProjectContext.Provider value={projectId}>`).
* `getProjectFlatView(projectId)` — плоский вид ЛЮБОГО проекта (не только активного), кэшированный по стабильной ссылке на `state.projects[projectId]`.
* `useProjectSelector(selector)` / `useProjectDispatch()` — берут `projectId` из `ProjectContext`; запись в проект, отличный от активного, автоматически заворачивается в `FOR_PROJECT`.
* `useProjectStore()` (2026-08-29) — аналог `useStore()` (весь плоский вид проекта + `dispatch`, без селектора), но привязанный к `ProjectContext`. Используется в `AIAgentNodeContent.js`: узел-ассистент рендерится внутри `NodeView` конкретного проекта и обязан читать/писать СВОЙ проект, даже если активен другой. До этой правки узел использовал `useStore()` и в неактивном проекте молча читал и менял активный проект — баг того же семейства, что чинили для окон уровней.

Панели (`Toolbar.js`, `Library.js`, `ContextActionBar.js`) на проектно-адресный слой НЕ переведены — используют обычные `useStore()`/`useSelector()`/`useDispatch()` и осознанно работают только с активным проектом.

**Защитный барьер до PLAN V7.** Межпроектные связи и перенос узлов между проектами сейчас невозможны: резолвер целей связи в `Port.js` ищет кандидатов через `getProjectFlatView(sourceProjectId)`, то есть физически не видит сущности чужих проектов — это не отдельная проверка «отклонить разные `projectId`», а суженное поле зрения поиска. Снятие барьера, межпроектные связи, Drag&Drop между проектами и импорт/экспорт всей рабочей области — предмет отдельного плана `PLAN_V7_CROSS_PROJECT_INTERACTIONS_AND_IO.md`, в кодовой базе пока не реализовано.

#### Выделение контейнеров и их изоляция

| Экшен | Payload | Описание |
|---|---|---|
| `TOGGLE_CONTAINER_ISOLATION` | `{ kind: 'project'\|'window', id }` | Включить/снять изоляцию контейнера на общем холсте |
| `SET_CONTAINER_ISOLATION` | `{ projectIds, windowIds }` | Задать набор изолированного целиком |
| `CLEAR_CONTAINER_ISOLATION` | — | Снять изоляцию контейнеров |

**Адресация в `selectedIds`.** Контейнеры адресуются префиксами: `project:<projectId>` и `window:<windowId>` (по стабильному id окна — номер уровня не уникален между проектами). Старые формы `'project'` и `level-window-<N>` продолжают распознаваться и означают контейнеры активного проекта.

**Классы выделения взаимоисключающи.** В `selectedIds` лежат либо только контейнеры, либо только сущности графа: добавление элемента другого класса ЗАМЕНЯЕТ выделение. Благодаря этому `DELETE_SELECTED` всегда однозначен — он либо удаляет контейнеры (проекты целиком, уровни сверху вниз одним шагом истории), либо сущности.

#### Пакетная история

| Экшен | Payload | Описание |
|---|---|---|
| `BEGIN_HISTORY_BATCH` | `{ logMessage? }` | Открыть пакет: последующие экшены НЕ пишут свои шаги в историю |
| `COMMIT_HISTORY` | `{ snapshot?, logMessage? }` | Закрыть пакет одним шагом Undo |
| `CANCEL_HISTORY_BATCH` | — | Закрыть пакет без записи (аварийный выход) |

Серия экшенов от ИИ применяется именно так: весь ответ ассистента — **один** шаг Undo. Без этого батч из 30+ команд вымывал лимит истории в 20 шагов, и отменить работу ассистента было невозможно.

---

### 5. Пример сгенерированного графа (JSON Protocol)

```json
[
  {
    "type": "ADD_LAYER",
    "payload": {
      "id": "layer-1-ui",
      "name": "1. UI Layer",
      "content": "Пользовательский интерфейс и представление",
      "color": "#0284c7",
      "position": { "x": -400, "y": -250 },
      "size": { "w": 600, "h": 400 },
      "parentId": "root"
    }
  },
  {
    "type": "ADD_NODE",
    "payload": {
      "id": "node-canvas-view",
      "name": "Canvas Viewport",
      "content": "Интерактивная область холста",
      "color": "#0f172a",
      "position": { "x": 30, "y": 80 },
      "size": { "w": 250, "h": 120 },
      "parentId": "layer-1-ui",
      "shape": "rectangle"
    }
  },
  {
    "type": "ADD_NODE",
    "payload": {
      "id": "node-store",
      "name": "Store Provider",
      "content": "Хранилище состояния",
      "color": "#0f172a",
      "position": { "x": 310, "y": 80 },
      "size": { "w": 250, "h": 120 },
      "parentId": "layer-1-ui",
      "shape": "rectangle"
    }
  },
  {
    "type": "ADD_PORT",
    "payload": {
      "id": "port-canvas-out",
      "nodeId": "node-canvas-view",
      "type": "output",
      "edge": "right",
      "position": 0.5,
      "name": "Events Dispatch",
      "color": "#38bdf8"
    }
  },
  {
    "type": "ADD_PORT",
    "payload": {
      "id": "port-store-in",
      "nodeId": "node-store",
      "type": "input",
      "edge": "left",
      "position": 0.5,
      "name": "Actions Handler",
      "color": "#0284c7"
    }
  },
  {
    "type": "ADD_LINK",
    "payload": {
      "id": "link-canvas-to-store",
      "sourcePortId": "port-canvas-out",
      "targetPortId": "port-store-in",
      "name": "Redux Dispatch",
      "linkStyle": "orthogonal",
      "color": "#38bdf8",
      "context": "layer-1-ui"
    }
  }
]
```

### 6. Ключевые инварианты при генерации портов и связей:

1. **Порты ОБЯЗАТЕЛЬНЫ:** Нейросеть должна самостоятельно генерировать порты (`ADD_PORT`) для узлов, задавая им подписи `name`, тип `input`/`output`, грань `edge` (`left`, `right`, `top`, `bottom`) и HEX-цвет.
2. **Связи соединяют ТОЛЬКО Порты:** Экшен `ADD_LINK` принимает в `sourcePortId` и `targetPortId` **строго ID ранее созданных портов**, а не ID узлов.
3. **Авто-выравнивание элементов на слое:** Координаты нод на слое следует разносить по сетке (`{x: 30, y: 80}`, `{x: 310, y: 80}`). При добавлении узлов приложение автоматически применяет к слою выравнивание `GeometryUtils.getSmartPlacement`.

---

## 🧪 Обязательное тестирование после изменений

После любых изменений в логике, редюсере, геометрии или компонентах **ИИ-ассистент обязан выполнить две команды проверки**:

1. **Юнит-тесты Node.js:**
   ```bash
   cmd /c "node --test app/tests/*.test.js"
   ```
   *Все тесты должны проходить со статусом PASS (0 ошибок). Точное число намеренно не дублируется в документации — единственный источник правды это вывод самого прогона.*

2. **Статическая проверка типов TypeScript:**
   ```bash
   cmd /c "npx -y -p typescript tsc -p app/jsconfig.json"
   ```
   *Команда должна завершаться с нулевым кодом ошибки.*

---

## 📌 Памятка для ИИ по стилю внесения правок

* **Не нарушать работу Zero-Build:** Не использовать `require()` или `import` в браузерных React-компонентах.
* **Редьюсер не вызывает сам себя:** рекурсивный вызов `reducer(...)` внутри `const reducer = ...` ломает загрузку через babel-standalone (top-level `const` перестаёт быть виден другим скриптам). Общую логику выносить в top-level функции (пример: `applyRemoveLevelWindow`).
* **Кэш скриптов:** после правок компонентов поднимать метку версии `?v=...` у всех script-тегов в `app/index.html` — иначе браузер пользователя исполняет старый код.
* **Видимость сущностей** проверять ТОЛЬКО через `HierarchyUtils.isEntityVisible(id, state)` — единая точка истины для глобального (L0) и локальных «глаз». Не дублировать логику фильтрации в компонентах.
* **Цель создания элементов** («+», перетаскивание связи в окно) определять через `HierarchyUtils.getAddContext(state)`; при `ok:false` действие запрещено (массовое выделение или неоднозначная ветка).
* **Подписки на DOM-узлы условного рендера** (например, вьюпорт окна уровня, размонтируемый при сворачивании) вешать через callback-ref, а не `useEffect` с deps — слушатель должен следовать за жизненным циклом узла.
* **Использовать GitHub Flavored Markdown links:** При ответах пользователю создавать кликабельные ссылки на файлы вида `[basename](file:///absolute/path/to/file)`.
* **Сохранять docstrings и комментарии:** Не удалять существующие комментарии, описывающие бизнес-логику и математику выталкивания.
* **Использовать атомарные edits:** При правках файлов использовать точечную замену `replace_file_content` или `multi_replace_file_content`.
