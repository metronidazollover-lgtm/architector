# Инструкции для ИИ-ассистентов (AGENTS.md)

Данный документ содержит ключевые архитектурные правила, спецификацию структуры и стандарты разработки для приложения **Architector**. Все ИИ-модели, работающие с этой кодовой базой, должны строго соблюдать изложенные здесь соглашения.

---

## 🏛️ Архитектурная философия (Zero-Build)

1. **Отсутствие бандлера и сборки (Zero-Build):**
   * Приложение работает напрямую в браузере без Webpack, Vite, Rollup или Babel CLI.
   * Все глобальные библиотеки (React 18, ReactDOM, Babel Standalone, Tailwind CSS Play CDN, Lucide Icons) подключаются через CDN в `app/index.html`.
   * Компоненты подключаются тегами `<script type="text/babel" src="...">` в `app/index.html`.
   * **КРИТИЧЕСКОЕ ПРАВИЛО:** В файлах компонентов **НЕЛЬЗЯ использовать синтаксис ES-модулей `import` / `export`** для браузерного скриптинга. Все компоненты и утилиты объявляются в общем глобальном скоупе Babel.

2. **Двойной экспорт утилит (Browser + Node.js):**
   * Файлы `app/utils/geometry.js`, `app/utils/hierarchy.js` и `app/store/reducer.js` исполняются как в браузере, так и в средах без DOM (в тестовом раннере `node:test`).
   * В конце каждого такого файла используется схема двойного экспорта:
     ```js
     if (typeof module !== 'undefined' && module.exports) {
         module.exports = GeometryUtils;
     } else {
         window.GeometryUtils = GeometryUtils;
     }
     ```

---

## 🗺️ Карта проекта и назначение файлов

```
architector-main/
├── app/
│   ├── index.html                 # Главная точка входа, подключение всех скриптов и CDN
│   ├── app.js                     # Корневой компонент с ErrorBoundary и вызовом ReactDOM.render
│   ├── server.py                  # Python dev-сервер с заголовками no-cache
│   ├── run.bat                    # Скрипт автозапуска сервера и открытия браузера
│   ├── store/
│   │   ├── reducer.js             # Чистая логика состояния, initialState, миграции (без JSX)
│   │   ├── engine.js              # Движок стора без React: подписки, кэш селекторов, профилировщик рендеров
│   │   └── Store.js               # React Context Provider + localStorage + fallback памяти
│   ├── utils/
│   │   ├── hierarchy.js           # v14: глубина и родство через parentId (getDepth), геометрия
│   │   │                          #   дорожка+окно (laneRect/nodeWorldRect/getWorldTransformV14),
│   │   │                          #   резолвер Drag&Drop (resolveDropTarget), нотация состояния
│   │   │                          #   (dumpNotation/parseNotation). Старые v13-функции (getEntityLevel,
│   │   │                          #   getWindowOfLevel, getDropTarget, isEntityVisible и т.д.) физически
│   │   │                          #   остаются в файле — их всё ещё вызывает migrateToV13-цепочка
│   │   │                          #   (см. §7.14 плана Lanes_v14) — но ни один живой компонент их не использует
│   │   └── geometry.js            # Геометрия портов, построение путей связей, авторасстановка, коллизии
│   ├── components/
│   │   ├── Canvas.js              # Мировой холст, жесты, Ctrl/Cmd+колесо = мировой зум, хоткеи,
│   │   │                          #   CrossWindowLinkLayer (связи между дорожками, мировые координаты),
│   │   │                          #   DragOverlay (перетаскиваемый узел поверх clip дорожки)
│   │   ├── LaneWindow.js          # v14: окно — набор дорожек рядом; шапка окна целиком (заголовок из
│   │   │                          #   имён дорожек, изоляция, разворот на весь экран, закрытие),
│   │   │                          #   перетаскивание/масштабирование/панорама/зум окна
│   │   ├── Lane.js                # v14: одна дорожка (внутренность узла или корня) — своя шапка
│   │   │                          #   (цвет/имя/путь, глаз, отстыковать, закрыть), тело с детьми-узлами
│   │   ├── Frame.js               # v14: рамка — рисуется кусками (один на дорожку с видимыми членами);
│   │   │                          #   ярлык с именем/счётчиком/⧉ только на куске в homeLaneId
│   │   ├── Node.js                # Отрисовка узла; position — ЛОКАЛЬНА дорожке родителя напрямую
│   │   │                          #   (без накопления через цепочку слоёв, как в v13)
│   │   ├── Port.js                # Точки привязки связей на гранях узла ИЛИ рамки (кусок в её дорожке)
│   │   ├── Link.js                # Связи ЦЕЛИКОМ внутри одной дорожки (локальные координаты);
│   │   │                          #   межлановые/межоконные — CrossWindowLinkLayer в Canvas.js
│   │   ├── NodePreview.js         # Компонент превью контента ноды при семантическом масштабировании
│   │   │                          #   (не подключён нигде — уже было мёртвым кодом до v14)
│   │   ├── ContextActionBar.js    # Верхняя контекстная панель свойств (Узлы, Рамки, Окна, Порты, Связи, Массовое)
│   │   ├── Toolbar.js             # Плавающий FAB справа: контекст «+» = activeLaneId/дорожка выделенного
│   │   │                          #   узла/root; «Рамка» и «Новое окно» не зависят от контекста выделения
│   │   ├── Library.js             # Левый обозреватель проекта (вкладки: Объекты, История; дерево + рамки)
│   │   ├── OutlinerTree.js        # Дерево узлов по parentId, поиск по имени, секция «Рамки»
│   │   └── AIAgentNodeContent.js  # Виджет ИИ-ассистента для нод типа 'ai-agent'; состояние — в НОТАЦИИ
│   │   │                          #   (HierarchyUtils.dumpNotation), не сырым JSON
│   │
│   │   # v14: LevelWindow.js/Layer.js физически ОСТАЮТСЯ в репозитории (не загружаются
│   │   #   в index.html) до Фазы 6 перехода на Дорожки/Окна/Рамки — см. docs/LANES_MODEL.md
│   ├── bench/
│   │   ├── core.bench.js          # Замер чистой логики на сценах S/M/L
│   │   ├── render.bench.js        # Замер перерисовок и FPS в headless-браузере
│   │   ├── syntax-check.js        # Шлюз: разбор всех JSX-файлов Babel до открытия страницы
│   │   └── offline-harness.js     # Стенд с локальными копиями CDN-библиотек
│   ├── types/
│   │   ├── typedefs.js            # Центральные JSDoc-типы (NodeEntity, LayerEntity, LinkEntity)
│   │   └── globals.d.ts           # Глобальные TypeScript-декларации для window/module утилит и компонентов
│   └── tests/
│       ├── reducer.test.js        # Юнит-тесты редьюсера: Undo/Redo, уровни (удаление/очистка/сдвиг),
│       │                          #   изоляция веток, getAddContext, прокси-порты, Drag&Drop
│       ├── coords.test.js         # Юнит-тесты координатного ядра уровней
│       ├── geometry.test.js       # Юнит-тесты авторасстановки и выталкивания
│       ├── hierarchy.test.js      # Юнит-тесты иерархии и защиты от зацикливания
│       ├── migration.test.js      # Юнит-тесты миграций v9 → v10 → v11, migrateToV13, REPARENT_ENTITY
│       ├── persistence.test.js    # Персистентность сессий ИИ, безопасность, пакет истории, кэш и индексы
│       ├── engine.test.js         # Движок стора: подписки, отсечение пустых dispatch, кэш селекторов
│       ├── reactivity.test.js     # «Рябь»: изменение предка обязано доходить до потомка
│       ├── selection.test.js      # Выделение контейнеров, изоляция, массовое удаление
│       └── fixtures/generate.js   # Детерминированный генератор сцен для бенчмарков и тестов
└── docs/
    ├── ARCHITECTURE.md            # Детальная архитектурная спецификация и инварианты
    ├── CHANGELOG.md               # История изменений и рефакторингов (источник правды по датам)
    ├── MIGRATIONS.md              # Документация конвертации форматов данных
    └── UX.md                      # Спецификация хоткеев, жестов и пользовательского опыта
    # PLAN.md удалён (2026-08-30, см. CHANGELOG.md): не обновлялся с 2026-08-22 и
    # разошёлся с кодовой базой. Планирование по фичам — в отдельных файлах
    # «Отчеты, аудиты, планы/планы/PLAN_*.md» (там же лежит PLAN_LAYERS_AND_CONTEXT_CREATION.md,
    # на который ссылаются правки этой сессии); история уже сделанного — в docs/CHANGELOG.md.
```

---

## 📐 Модель данных v14 (Дорожки/Окна/Рамки) и Инварианты

**Формат сохранения `formatVersion: 14`.** Активирована с Фазы 4 перехода (`getInitialMultiState` вызывает `migrateToV14(migrateToV13(...))` — композиция, а не замена, см. §7.14 «Отчеты, аудиты, планы/Lanes_v14/PLAN_V14_LANES.md»). Целевая спецификация поведения — `docs/LANES_MODEL.md`; здесь — только сводка инвариантов, за которой должен свериться агент перед правкой кода.

1. **Три сущности вместо одной запутанной (`docs/LANES_MODEL.md` §1):**
   * **Дорожка (lane)** — внутренность одного узла (или корня проекта): не хранится, производная от `parentId`. Ширина — константа (`HierarchyUtils.LANE_W`=420, `ROOT_LANE_W`=520 у корня, `HIDDEN_LANE_W`=26 у свёрнутой «глазом»).
   * **Окно (window)** — набор дорожек, положенных рядом (`state.windows`, ключ — стабильный id). Своя камера `window.camera: {offset, zoom}`. `position`/`size`/`camera`/`collapsed` — вне истории Undo; структурные действия (открыть/закрыть/состыковать дорожку, закрыть/развернуть окно) — В истории (§9 LANES_MODEL.md, уточнение §7.6 плана).
   * **Рамка (frame, заменяет `layers`)** — множество узлов из любых дорожек (`state.frames`), рисуется кусками (по bbox её членов в каждой дорожке + отступ), не имеет `position`/`size`/`parentId` и не участвует в родстве.
2. **Единственная структура — `parentId ∈ {'root', nodeId}`.** Глубина узла — длина цепочки `parentId` до `'root'` (прямые дети корня — глубина 1), вычисляется `HierarchyUtils.getDepth(id, nodes)`. Полей `ownerId`, `ownerGap`, `homeLevel`, `levelWindows`, `levelViews`, `levelFocusParentId`, `levelHideNeighbors`, `activeLevelIndex` в v14-сущностях и v14-состоянии проекта больше нет (их место — `activeLaneId`/`activeFrameId`). `parentId`, указывающий на рамку или окно, недопустим в любом пути создания/переноса.
3. **Мировые координаты узла = окно (`window.position` + `window.camera`) + смещение дорожки его родителя в этом окне (`HierarchyUtils.laneOffset`, порядок в `window.lanes`) + собственная `node.position` (локальна дорожке РОДИТЕЛЯ, без накопления через цепочку контейнеров, как было в v13 через слои).** `HierarchyUtils.getWorldTransformV14`/`nodeWorldRect`/`nodeRectInWindow` считают это за агента; `Node.js`/`Frame.js`/`Link.js` внутри одной дорожки используют `position` НАПРЯМУЮ как CSS `left`/`top` — Lane.js уже поместил их в контейнер, трансформированный камерой окна.
4. **Режим Drag&Drop (`ui.dragDropMode`, предохранитель, смысл не изменился).** Выключен (дефолт при каждой загрузке): карточка клампится границей своей дорожки (мягкая остановка, без отскока — клампинг в `MOVE_SELECTED`, `reducer.js`), `parentId` сменить нельзя. Включён: сквозной перенос между дорожками/окнами/проектами (**однопроектный** резолвер `HierarchyUtils.resolveDropTarget` — `Node.js` пока не сканирует другие проекты и никогда не передаёт `sourceProjectId`/`targetProjectId` в `REPARENT_ENTITY`, так что живого UI-пути к кросс-проектному переносу карточки всё ещё нет; сам `applyCrossProjectReparent`, если вызвать его напрямую, уже переписан на v14 в Фазе 5 — см. §5 плана и подраздел «Кросс-проектный Drag&Drop» ниже). `resolveDropTarget(pointerWorld, draggedIds, state, opts) → {ok, windowId, ownerId|nodeId, frameId?, isMove?}` — единственный резолвер: окно → дорожка → карточка → кусок рамки. Deep (`mode:'deep'`, по умолчанию, ветка едет целиком) / Shallow (`mode:'shallow'`, `Alt`, только сама сущность — прямые дети усыновляются прежним родителем с `findFreePosition`) — оба через `REPARENT_ENTITY`; **подтверждающего диалога на дропе больше нет** (сам тумблер DnD — единственный предохранитель, `buildTransferConfirmText` не вызывается). `Canvas.js`'s `DragOverlay` перерисовывает перетаскиваемую ветку поверх всех окон в мировых координатах, пока идёт жест — иначе `overflow-hidden` тела дорожки обрезал бы карточку на границе.
5. **Удаление узла (`REMOVE_NODE`)** каскадно удаляет всю ветку потомков по умолчанию (было — «плоское» удаление без потомков в v13); `keepChildren: true` — прямые дети усыновляются дедом с `findFreePosition`. **Удаление рамки (`REMOVE_FRAME`)** не трогает узлы — только саму рамку и её порты/связи. **Закрытие окна/дорожки** (`CLOSE_WINDOW`/`CLOSE_LANE`) — чисто обзорное действие, данные не меняет; связи между узлами, чьи дорожки были в закрытом окне, сохраняются и переотрисовываются, как только дорожки где-либо снова открыты. Сирот-якорей, ре-якорения «внук — деду» и сдвига глубин при удалении в v14 не существует — эти понятия принадлежали модели «общий холст на уровень», от которой Дорожки/Окна/Рамки как раз и уходят (диагноз — §0.1 плана).
6. **Ключи словарей:**
   * В объектах `nodes`, `frames`, `ports`, `links`, `windows` ключ в словаре **ОБЯЗАТЕЛЬНО** совпадает с `entity.id` (например, `nodes["node-1"].id === "node-1"`).

3. **Форма нод (`shape`):**
   * Все узлы имеют стандартизированную прямоугольную форму `shape: "rectangle"`.

7. **Предотвращение двойного смещения:**
   * При перемещении родителя его потомки перемещаются автоматически за счет относительных координат. При массовом выделении проверяется `HierarchyUtils.isDescendantOfV14`, чтобы дочерние элементы не сдвигались дважды.

8. **Разделение явного выделения и визуальной подсветки:**
   * Обработчики событиий (`handleMouseDown`) и логика Redux проверяют `isExplicitlySelected = state.selectedIds.includes(id)`.
   * Визуальный рендеринг компонентов (`isSelected`) учитывает косвенное подсвечивание соединенных сетей связей/портов/узлов. `selectedIds` при клике хранит строго выбранную сущность для единичного редактирования свойств.

9. **Порядок скриптов в `index.html`:**
   * Скрипты подключаются strictly по цепочке: `utils/` → `store/` → `components/` → `app.js`. Новый компонент обязательно регистрируется в `app/index.html`.

10. **Визуализация портов и сетей связей:**
    * Стандартный размер порта: `w-3 h-3` (12px).
    * При выделении или подсвечивании соединенной сети порт масштабируется с коэффициентом `scale-[2.1]`.
    * **Порты и связи рамок (v14).** Рамка — полноправный участник графа связей наравне с узлом, но порты живут ТОЛЬКО на куске в её `homeLaneId` (если там сейчас нет видимых членов — временно на первом непустом куске, `homeLaneId` не переписывается). Хост-сущность порта разрешается полиморфно (`state.nodes[id] || state.frames[id]`) в `HierarchyUtils.getPortWorldPositionV14`/`Port.js`/`Frame.js`/`Link.js`. Удаление рамки каскадно удаляет её порты и подключённые связи.

11. **Управление свойствами через верхнюю контекстную панель (Context Action Bar):**
    * Все свойства узлов (цвет, значок, медиа-ссылка, добавление в рамку, привязка к сетке, удаление, типографика) настраиваются через единую верхнюю контекстную панель свойств.

12. **Динамический авторесайз и перенос текста (Live Auto-Sizing):**
   * При инлайн-редактировании имени (`name`) и описания (`content`) размеры узла пересчитываются мгновенно на лету (`onChange`, `skipHistory: true`).
   * Для заголовка поддерживается перенос строк (`break-all`, `nameLines`) с автоматическим увеличением высоты шапки и узла до ширины A4 (794px).
   * В обычном авторежиме полосы прокрутки отсутствуют, а кастомный скроллбар (`custom-scrollbar`) появляется только при ручном сжатии узла пользователем (`userResized: true`).

---

## 🤖 Спецификация и протокол управления для внешних ИИ-Агентов (API & Function Calling Protocol)

Данный раздел является **официальной спецификацией для подключенных внешних ИИ-агентов (LLM через OpenAI API, Anthropic Claude, Gemini или локальные нейросети)**, которые генерируют или модифицируют графы в приложении Architector.

### 1. Формат выполнения команд

Любые изменения графа со стороны ИИ-агента передаются в виде **массива Redux-экшенов в JSON-блоке** в конце текстового ответа:
```json
[
  { "type": "ACTION_NAME", "payload": { ... } }
]
```

### 2. Спецификация сущностей и типов данных

#### 🖼️ Рамки (`frame`, v14, заменяет `layer`)
Рамка — множество узлов из любых дорожек (см. `docs/LANES_MODEL.md` §1/§4.2). Не имеет `position`/`size`/`parentId` — рисуется кусками, по одному bbox на дорожку с видимыми членами.
* `id` *(string, обязательный)*: уникальный ID (например `"frame-ui"`).
* `name` *(string, обязательный)*: заголовок рамки.
* `content` *(string)*: краткое описание назначения рамки.
* `color` *(string)*: HEX-цвет контура и ярлыка (например `"#0284c7"`).
* `fontFamily` *(string, опционально)*: гарнитура шрифта.
* `fontSize` *(number, опционально)*: размер шрифта ярлыка (11..24, по умолчанию 14).
* `members` *(string[], обязательный)*: id узлов-членов; порядок = порядок добавления.
* `homeLaneId` *(string)*: `'root'` или id узла — дорожка «первого куска» (там ярлык и порты рамки); `null`, пока в рамке нет ни одного члена.
* `snapToGrid` *(boolean)*: удержание сетки при перемещении куска (`MOVE_FRAGMENT`, всегда `true`).

#### 📦 Узлы (`node`)
Узел — центральный компонент логики или структуры.
* `id` *(string, обязательный)*: уникальный ID (например `"node-viewport"`).
* `name` *(string, обязательный)*: название узла.
* `content` *(string)*: текстовое или маркдаун описание.
* `color` *(string)*: HEX-цвет фона шапки узла.
* `fontFamily` *(string, опционально)*: гарнитура шрифта (например `"Fira Code, monospace"`).
* `fontSize` *(number, опционально)*: кегль шрифта (11..24, по умолчанию 14, динамически масштабирует шапку и узел).
* `position` *({x: number, y: number}, обязательный)*: координаты относительны родителя.
* `size` *({w: number, h: number}, обязательный)*: габариты (автоподстройка по умолчанию, базовая ширина 220-300px).
* `parentId` *(string)*: `"root"` или ID узла-родителя (v14: единственная структура родства — id рамки/окна как `parentId` недопустим).
* `shape` *(string)*: форма узла — `"rectangle"` (по умолчанию).
* `type` *(string)*: `"default"` (обычный) или `"ai-agent"` (интерактивный ИИ-копилот).
* `icon` *(string, опционально)*: иконка Lucide (`"icon-box"`, `"icon-bot"`) или эмодзи (`"🤖"`).
* `mediaUrl` *(string, опционально)*: URL картинки для медиа-карточки.
* `mediaHeight` *(number, опционально)*: высота картинки в пикселях (например `80`).

#### 🔌 Порты (`port`)
Точка привязки линий на гранях узла ИЛИ рамки — рамка полноправно участвует в графе связей наравне с узлом (см. §10 в разделе «Модель данных»).
* `id` *(string, обязательный)*: уникальный ID (например `"port-vp-out"`).
* `nodeId` *(string, обязательный)*: ID сущности-владельца — узла **или рамки**. Несмотря на имя поля (легаси), значением может быть как `node.id`, так и `frame.id` (порт рисуется на её куске в `homeLaneId`).
* `type` *(string)*: `"input"` (вход) или `"output"` (выход).
* `edge` *(string)*: грань узла — `"left"`, `"right"`, `"top"`, `"bottom"`.
* `position` *(number 0.0..1.0)*: смещение вдоль грани (0.5 = середина).
* `name` *(string)*: подпись порта.
* `content` *(string, опционально)*: описание передаваемых данных порта.
* `fontFamily` *(string, опционально)*: гарнитура шрифта.
* `fontSize` *(number, опционально)*: размер шрифта (по умолчанию 12).
* `color` *(string)*: HEX-цвет точки привязки.

#### ⚡ Связи (`link`)
Линии, соединяющие порты. Так как порт может принадлежать и узлу, и рамке, связь допустима в любой комбинации: Узел ↔ Узел, Рамка ↔ Рамка, Узел ↔ Рамка.
* `id` *(string, обязательный)*: уникальный ID (например `"link-ui-store"`).
* `sourcePortId` *(string, обязательный)*: ID порта-источника.
* `targetPortId` *(string, обязательный)*: ID порта-приемника.
* `name` *(string)*: подпись связи (например `"HTTP Dispatch"`).
* `content` *(string, опционально)*: описание назначения связи.
* `linkStyle` *(string)*: `"orthogonal"` (прямые углы) или `"bezier"` (плавная кривая).
* `fontFamily` *(string, опционально)*: гарнитура шрифта.
* `fontSize` *(number, опционально)*: размер шрифта (по умолчанию 12).
* `color` *(string)*: HEX-цвет линии.
* `context` *(string)*: контекст отображения (обычно совпадает со слоем или `"root"`).

---

### 3. Математика координат (v14)

1. **`position` узла — ЛОКАЛЬНА дорожке его родителя** (`parentId`), а не мировому холсту и не накапливается через цепочку контейнеров, как в v13 через слои. Безопасный диапазон внутри дорожки (`LANE_W`=420, у корня `ROOT_LANE_W`=520): `x: 20..350`, `y: 20..600`, братья разносятся сеткой с шагом ~130 по y или ~280 по x при нескольких колонках.
2. **Дорожка отображается только если её открыли явно** (`OPEN_LANE`/окно уже содержит `ownerId` в `window.lanes`) — узел без открытой дорожки родителя не отображается и не участвует в рендере. `CREATE_NESTED_NODE` — единственное исключение из «открывается только явным действием»: сама открывает дорожку родителя, если её ещё нигде нет.
3. **Родитель узла — единственное поле `parentId`** (`'root'` или id узла), назначается при создании и при `REPARENT_ENTITY`. Мировые координаты (для Drag&Drop, связей между дорожками) — `HierarchyUtils.getWorldTransformV14`/`nodeWorldRect(nodeId, state, preferWindowId?)`, которые сами находят окно, где сейчас открыта дорожка узла.

---

### 4. Поддерживаемые Redux-экшены (API Спецификация)

> **✅ v14 активирована (конец Фазы 4 перехода, см. «Отчеты, аудиты, планы/Lanes_v14/PLAN_V14_LANES.md» §7.14).** `getInitialMultiState` строит состояние композицией `migrateToV14(migrateToV13(...))` — живая загрузка теперь v14-форма (`nodes`/`frames`/`windows`, без `layers`/`levelWindows`/`levelViews`). Реестр дорожек/окон/рамок ниже — единственный живой путь для этих сущностей. Несколько чисто-визуальных v13-экшенов (`MOVE_LEVEL_WINDOW`, `RESIZE_LEVEL_WINDOW`, `ALIGN_LEVEL_WINDOWS`, `TOGGLE_LEVEL_COLLAPSE`, `UPDATE_LEVEL_PROPERTIES`, `PAN_LEVEL_WINDOW`, `ZOOM_LEVEL_WINDOW`) физически остаются в `reducer.js` нетронутыми (см. §7.12/§7.14 плана), но **ни один живой компонент их больше не диспатчит** — `LevelWindow.js`/`Layer.js` не загружаются с Фазы 4. `migrateToV13`/`normalizeLevelWindows`/`migrateProjectEntitiesToV13` и их зависимости в `hierarchy.js` (`getEntityLevel`, `getWindowOfLevel`, `getDropTarget`, `isEntityVisible` и др.) остаются в кодовой базе НАВСЕГДА как постоянное звено цепочки миграций (`migrateToV10 → … → migrateToV13 → migrateToV14`) — не трогать. **Кросс-проектный `REPARENT_ENTITY` переписан на v14 в Фазе 5** (барьер `formatVersion >= 14` снят — `isDescendantOfV14`/`canReparentToV14`, только узлы, окна целевого проекта через `applyOpenLaneV14`); живого UI-пути к нему по-прежнему нет — `Node.js` дропает только внутри своего проекта (`HierarchyUtils.resolveDropTarget` не сканирует другие проекты), см. подраздел «Кросс-проектный Drag&Drop» ниже. `docs/LANES_MODEL.md` — целевая спецификация поведения v14.

| Экшен | Payload | Описание |
|---|---|---|
| `ADD_NODE` | `{ id, name, content, color, position, size, parentId, shape: "rectangle", type, mediaUrl, mediaHeight, icon?, fontFamily?, fontSize? }` | Добавить прямоугольный узел. **v14:** `parentId` только `'root'` или id узла (id слоя больше не принимается — авторасстановка внутри слоя и достройка окна уровня убраны) |
| `ADD_PORT` | `{ id, nodeId, type, edge, position, name, color, content?, fontFamily?, fontSize? }` | Добавить порт на грань узла, слоя (v13) ИЛИ рамки (v14 — `nodeId`, легаси-имя поля, может быть id рамки) |
| `ADD_LINK` | `{ id, sourcePortId, targetPortId, name, linkStyle, color, context, content?, fontFamily?, fontSize? }` | Создать связь между портами |
| `CREATE_NESTED_NODE` | `{ parentId, name?, color?, shape?, type?, id? }` | Создать вложенный узел с `parentId` напрямую на родителя. **v14:** вместо достройки окна уровня — дорожка родителя открывается автоматически, если её ещё нигде нет (`OPEN_LANE` внутри, §0.4.3 плана) |
| `MOVE_LEVEL_WINDOW` | `{ index, windowId?, dx?, dy?, position?, skipHistory? }` | **v13, не в истории.** Переместить пространственное окно уровня. **При нескольких открытых проектах передавайте `windowId`**: `index` (номер уровня) одинаков в каждом проекте, поэтому без `windowId` `multiReducer` не может определить окно-владельца и по умолчанию адресует экшен АКТИВНОМУ проекту, даже если нужное окно принадлежит другому |
| `RESIZE_LEVEL_WINDOW` | `{ index, windowId?, size: { w, h }, skipHistory? }` | **v13, не в истории.** Изменить габариты пространственного окна уровня. Та же оговорка про `windowId`, что и у `MOVE_LEVEL_WINDOW` |
| `ALIGN_LEVEL_WINDOWS` | - | **v13, в истории.** Автоматически выстроить окна уровней вертикально по порядку L0..LN |
| `TOGGLE_LEVEL_COLLAPSE` | `{ index, windowId?, id? }` | **v13, не в истории.** Свернуть/развернуть окно уровня до шапки. **Передавайте `windowId` / `id`** при нескольких открытых проектах — см. оговорку у `MOVE_LEVEL_WINDOW` |
| `UPDATE_PROJECT_PROPERTIES` | `{ updates: { projectName, projectColor, projectFontFamily, projectContent } }` | Обновить глобальные свойства проекта |
| `UPDATE_LEVEL_PROPERTIES` | `{ index, windowId?, updates: { name, color, fontFamily, fontSize, content, innerOffset, innerZoom } }` | **v13.** Обновить свойства конкретного окна уровня (в т.ч. зум/панорамирование внутренней камеры окна — `innerZoom`/`innerOffset`). Пишет в историю, только если изменились свойства РАМКИ окна (имя/цвет/…) — чисто камерные поля никогда не историятся. **Передавайте `windowId`** при нескольких открытых проектах — см. оговорку у `MOVE_LEVEL_WINDOW` |
| `CLEAR_PROJECT` | — | **v14:** полная очистка проекта (узлы, рамки, связи, порты, ВСЕ окна — `windows: {}`). Ни одно окно больше не «переживает» очистку (было: окно L0 сохранялось в v13) — обозреватель проекта всегда даёт открыть корень заново (§10.7 LANES_MODEL.md) |
| `UPDATE_PROXY_PORT` | `{ linkId, windowId, edge, fraction, skipHistory? }` | Ручное положение прокси-порта межуровневой связи на рамке окна (пишется в `link.proxyOverrides[windowId]`; fraction клампится в 0.03..0.97) |
| `SET_DRAG_GESTURE` | `{ ids, target, mode? } \| null` | Транзиентный стейт жеста Drag&Drop (`state.dragGesture`): оверлей поверх окон и подсветка целей. `mode` — живой Deep/Shallow (Alt), обновляется во время жеста, а не только на старте. Вне истории и сохранений |
| `RESTORE_ENTITIES` | `{ nodes? }` | Откат Drag&Drop-жеста без записи истории: возврат словаря узлов к срезу mousedown (Esc, бросок в пустоту при включённом тумблере) |
| `UPDATE_NODE` | `{ id, updates: { name, content, color, mediaUrl, icon, fontFamily, fontSize } }` | Обновить свойства узла |
| `UPDATE_PORT` | `{ id, updates: { name, color, edge, position, content, fontFamily, fontSize } }` | Переместить, переименовать порт или настроить шрифт |
| `UPDATE_LINK` | `{ id, updates: { name, color, linkStyle, content, fontFamily, fontSize } }` | Изменить стиль, цвет, описание или шрифт связи |
| `REPARENT_ENTITY` | `{ ids: string[] \| id: string, targetParentId: string \| newParentId: string, mode?: 'deep' \| 'shallow', position?: { x, y }, positionsById?: {}, historySnapshot? }` | **Единственный экшен переноса.** **v14:** `targetParentId` — только `'root'` или id узла (`targetLevelIndex` и id слоя как цель убраны совсем). `mode: 'deep'` (по умолчанию) — сущность и её ветка переезжают вместе. `mode: 'shallow'` — переезжает только сама сущность, её ПРЯМЫЕ дети усыновляются её прежним родителем с авто-раздвижкой (`GeometryUtils.findFreePosition`); включается зажатием `Alt` во время перетаскивания. Проверка цикла и существования цели — `HierarchyUtils.canReparentToV14`/`isDescendantOfV14` (только узлы). Позиция: явный `position` (одиночный drop) или `positionsById` (множественный Drag&Drop) → иначе всегда `findFreePosition` (в v14 каждая дорожка — своя система координат, «то же окно» из v13 не существует). `historySnapshot` — срез на mousedown, весь Drag&Drop-жест — один шаг Undo. Обратная совместимость: одиночный `{ id, newParentId }` нормализуется в `{ ids: [id], targetParentId: newParentId }`. Дроп на карточку узла без открытой дорожки открывает её автоматически (§0.4.3 плана). Членство в рамках при переносе не меняется. |
| `DELETE_SELECTED` | без payload (работает по `state.selectedIds`) | **v14:** узлы — каскад всей ветки (как `REMOVE_NODE`); рамки — как `REMOVE_FRAME` (узлы остаются); окно (адресуется `window:<id>` в `selectedIds`) — `CLOSE_WINDOW` (только обзор). Сирот-якорей и ре-якорения не существует — см. §7 в разделе «Модель данных» |
| `CENTER_ON_ENTITY` | `id` | **v14, упрощено:** центрирует только МИРОВУЮ камеру (без отдельной подстройки камеры каждого окна, как было в v13). Если дорожка узла/рамки нигде не открыта — открывает её автоматически (§7.1.7 плана), это отдельный шаг истории, объединённый с открытием |
| `REMOVE_NODE` | `nodeId \| { id, keepChildren? }` | **v14:** по умолчанию каскадно удаляет всю ветку потомков (было: «плоское» удаление без потомков), их порты/связи и членство в рамках. `keepChildren: true` — прямые дети усыновляются дедом с `findFreePosition` (Shallow-подобная семантика), поддеревья детей не меняются |
| `REMOVE_PORT` / `REMOVE_LINK` | `id` | Прицельно удалить конкретный порт или конкретную связь |
| `MASS_UPDATE` | `{ ids: string[], updates?: {}, updatesById?: {} }` | Массово обновить свойства группы элементов — узлов, **рамок (v14)**, портов, связей (цвет, шрифт, размер, сетка) |
| `CREATE_AI_SESSION` | `{ nodeId }` | Создать новую изолированную сессию диалога для узла ассистента |
| `SWITCH_AI_SESSION` | `{ nodeId, sessionId }` | Переключить активную сессию диалога узла |
| `DELETE_AI_SESSION` | `{ nodeId, sessionId }` | Удалить сохраненную сессию диалога узла |
| `ADD_AI_MESSAGE` | `{ nodeId, message: { role, content, media? } }` | Добавить сообщение в активную сессию диалога узла |
| `CLEAR_AI_HISTORY` | `{ nodeId? }` | Очистить историю сообщений узла или всех узлов |
| `UPDATE_AI_SETTINGS` | `{ provider?, model?, baseUrl?, mode?, confirmMode?, contextMode? }` | Обновить параметры ИИ-ассистента (`confirmMode: 'ask' \| 'auto'`) |

#### Дорожки, окна и рамки (v14, живой реестр)

Реализован в `reducer.js`, покрыт тестами (`app/tests/reducer.test.js`) и подключён к живой загрузке с конца Фазы 4 (см. предупреждение выше). Работает с полями `state.nodes`/`state.frames`/`state.windows` — целевая модель поведения подробно описана в `docs/LANES_MODEL.md`.

| Экшен | Payload | Описание |
|---|---|---|
| `OPEN_LANE` | `{ ownerId, windowId? }` | Открыть дорожку узла (или `'root'`). С `windowId` — добавить в СУЩЕСТВУЮЩЕЕ окно (составное окно из нескольких дорожек); без него — создать НОВОЕ окно в колонке по глубине дорожки, но только если она ещё нигде не открыта (иначе no-op) |
| `CLOSE_LANE` | `{ windowId, ownerId }` | Убрать дорожку из окна. Данные не трогает. Окно, опустевшее после этого (нет дорожек и не окно рамки), закрывается автоматически |
| `DOCK_LANE` | `{ ownerId, fromWindowId, toWindowId, index? }` | Переставить дорожку из одного окна в другое (опционально на позицию `index`). Источник, опустевший после этого, закрывается автоматически |
| `DETACH_LANE` | `{ windowId, ownerId }` | Отстыковать дорожку в собственное новое окно. Источник, опустевший после этого, закрывается автоматически |
| `REORDER_LANE` | `{ windowId, ownerId, toIndex }` | Изменить порядок дорожек внутри окна. Вне истории (визуальная перестановка) |
| `TOGGLE_LANE_HIDDEN` | `{ windowId, ownerId }` | «Глаз»: схлопнуть/развернуть одну дорожку в одном окне до полоски. Вне истории |
| `OPEN_FRAME_WINDOW` | `{ frameId }` | Открыть рамку как окно: дорожки всех её членов, показаны только члены (`window.frameId`). No-op, если для рамки уже есть открытое окно |
| `CLOSE_WINDOW` | `{ windowId }` | Закрыть окно целиком. Только обзор — данные не трогает, связи/узлы сохраняются и переотрисуются, как только их дорожки где-либо снова откроются |
| `MOVE_WINDOW` | `{ windowId, dx?, dy?, position? }` | Переместить окно на холсте. Вне истории (чисто визуальное) |
| `RESIZE_WINDOW` | `{ windowId, size: { w, h } }` | Изменить размер окна, не меньше одной карточки. Вне истории |
| `PAN_WINDOW` | `{ windowId, offset }` | Панорамирование камеры окна. Вне истории |
| `ZOOM_WINDOW` | `{ windowId, zoom?, offset? }` | Зум (и опционально панорама) камеры окна. Вне истории |
| `TOGGLE_WINDOW_COLLAPSE` | `{ windowId }` | Свернуть/развернуть окно до шапки. Вне истории |
| `TOGGLE_WINDOW_MAXIMIZE` | `{ windowId, viewport? }` | Развернуть окно на весь вьюпорт; повторный вызов возвращает исходные `position`/`size` (хранятся в `window.preMaximize`). В истории |
| `NEW_EMPTY_WINDOW` | — | Создать пустое окно про запас, не привязанное ни к одной дорожке — НЕ закрывается автоматически, пока пользователь не закроет его сам. В истории |
| `ALIGN_WINDOWS` | — | Разложить все окна по колонкам глубины (колонка = глубина первой дорожки окна). В истории |
| `UPDATE_WINDOW_PROPERTIES` | `{ windowId, updates: { name?, color?, fontFamily?, fontSize?, offset?, zoom?, collapsed? }, skipHistory? }` | `offset`/`zoom`/`collapsed` — камера, вне истории всегда; остальные поля — свойства окна, в истории (если явно не `skipHistory`) |
| `SET_ACTIVE_LANE` | `ownerId` | Дорожка-контекст для FAB «+» (клик в дорожку). Вне истории |
| `SET_ACTIVE_FRAME` | `frameId` | Рамка, подсвеченная кликом по её ярлыку. Вне истории |
| `ADD_FRAME` | `{ members?, homeLaneId?, name?, color? }` | Создать рамку. Двойной режим (без отдельной кнопки — переиспользует существующую «Слой»/«Рамка», §7.9 плана): есть `members` — рамка сразу с этими узлами, `homeLaneId` выводится из дорожки первого члена; без `members` — пустая рамка (`homeLaneId: null` до первого `FRAME_ADD_MEMBERS`) |
| `UPDATE_FRAME` | `{ id, updates, skipHistory? }` | Обновить свойства рамки (имя, цвет, `homeLaneId` и т.д.) |
| `REMOVE_FRAME` | `id \| { id }` | Удалить рамку. Узлы-члены НЕ удаляются — только сама рамка, каскадно её порты и связи через них, и любое открытое окно этой рамки |
| `FRAME_ADD_MEMBERS` | `{ frameId, ids }` | Добавить узлы в members рамки. Если у рамки не было `homeLaneId` — выставляется по дорожке первого добавленного |
| `FRAME_REMOVE_MEMBERS` | `{ frameId, ids }` | Убрать узлы из members. Опустевшая рамка НЕ удаляется автоматически — остаётся именованной цветной заготовкой (§7.3.4 плана) |
| `MOVE_FRAGMENT` | `{ frameId, ownerId, dx, dy, skipHistory? }` | Сдвинуть всех членов рамки, лежащих в конкретной дорожке `ownerId` (один кусок рамки), на `dx`/`dy` |

#### Мультипроектность (v12)

| Экшен | Payload | Описание |
|---|---|---|
| `ADD_PROJECT` | `{ name? }` | Создать пустой проект правее существующих и сделать его активным |
| `ADD_PROJECT_FROM_FILE` | данные проекта | Импорт файла как ДОБАВЛЕНИЕ проекта (не замена) |
| `REMOVE_PROJECT` | `{ id? }` | Удалить проект целиком. Необратимо: межпроектной истории нет |
| `SET_ACTIVE_PROJECT` | `id` | Сделать проект активным (сбрасывает выделение) |
| `FOR_PROJECT` | `{ projectId, action }` | Доставить любой внутренний экшен в КОНКРЕТНЫЙ проект, не делая его активным |
| `LOAD_GLOBAL_STATE` | данные ВСЕГО рабочего пространства (Фаза 6.4) | Заменяет `projects`/`projectOrder`/`activeProjectId`/`projectCounter`/`crossProjectLinks` целиком. Деструктивно — необратимо |
| `MERGE_PROJECT_FROM_FILE` | данные проекта (Фаза 6.5) | Слить файл В АКТИВНЫЙ проект: каждая сущность получает свежий id, коллизии позиций на корне уровня разводятся автоматически |

**`LOAD_STATE` сам доводит файл любой версии до v14 (§7.15 плана, Фаза 5).** Единая точка миграции: после всей v13-предобработки (авто-размер узлов, `snapToGrid`, `resolveLayerCollisionsOnLoad`, умное размещение внутри слоёв, `resolveContextCollisions`, `normalizeLevelWindows`) `LOAD_STATE` дополнительно прогоняет результат через `migrateProjectEntitiesToV13` → `migrateProjectEntitiesToV14` и возвращает готовую v14-форму (`nodes`/`frames`/`windows`, `activeLaneId: null`, `activeFrameId: null`) — независимо от того, из какой версии реально пришёл файл. `ADD_PROJECT_FROM_FILE` просто диспатчит `LOAD_STATE` на пустую базу и берёт результат как есть — отдельной обёртки-миграции ему больше не нужно. `render.bench.js` строит свою синтетическую сцену тем же самым `LOAD_STATE`, так что автоматически получает v14-сцену без обхода.

**`MERGE_PROJECT_FROM_FILE`: ремап id и всегда-новые окна.** Файл сначала целиком проходит через `LOAD_STATE` (та же v14-миграция, что и выше), затем каждый узел/рамка/порт/связь получает свежий id (карта `oldId -> newId`, с префиксами `node-`/`frame-`/`port-`/`link-`); внутренние ссылки (`parentId` узла, `members`/`homeLaneId` рамки, `nodeId` порта, `sourcePortId`/`targetPortId`, `lanes`/`hidden`/`frameId` окна, `portId` внутри `externalGateways`) переписываются через неё. Целевой контекст для `'root'`-адресованного содержимого файла — `activeLaneId` активного проекта, если он указывает на живой узел, иначе тоже `'root'`. **Окна файла НЕ ищут себе пару для слияния (никакого якорения по `levelIndex`/глубине, в отличие от v13) — каждое всегда заводится заново** со свежим id через `newWindowId()`, затем колонкой сдвигается правее самого правого окна активного проекта (`projectWindowAnchor` + `PROJECT_SLOT_GAP`). Слияние узлов — на объединённом наборе (существующее содержимое активного проекта + новоприбывшее) через `GeometryUtils.resolveContextCollisions(mergedNodesRaw, {})` — вторым аргументом пустой словарь: v13-концепции слоя-координатного-контейнера в v14 нет, эта функция теперь группирует и раздвигает узлы чисто по `parentId`-контексту (дорожке). **Главный холст файла (`'root'`) мержится в целевой контекст активного проекта ЛИТЕРАЛОМ, а не явным id узла/окна** — иначе `resolveContextCollisions` не увидел бы общий контекст между исходным содержимым активного проекта и только что слитым. Импортированные `externalGateways` (portId уже перемаппленный) оседают в `pendingGateways` активного проекта, дальше — обычная `reconcilePendingGateways`: если совпадающая половина штекера лежит в ДРУГОМ проекте — связь пересобирается сразу.

**UI выбора варианта (`ContextActionBar.js`/`handleImportProject`).** Для файла, у которого нет `kind: 'global'`: `window.confirm` — ОК = слить с активным (`MERGE_PROJECT_FROM_FILE`), Отмена = добавить как новый проект (`ADD_PROJECT_FROM_FILE`, поведение по умолчанию, как и раньше). Оба «глобальных» и «слитных» экшена диспатчатся через `rawDispatch`, а не через локально-обёрнутый `dispatch` компонента — тот заворачивает большинство экшенов в `FOR_PROJECT`, адресуя одно-проектному `reducer`, у которого нет таких `case`.

**Глобальный экспорт/импорт рабочего пространства (Фаза 6.4).** `handleExportWorkspace`/`handleImportWorkspace` (`ContextActionBar.js`) — в отличие от `handleExportProject`/`ADD_PROJECT_FROM_FILE` (один проект, режим «добавить»), эти работают с ВСЕМ пространством разом и в режиме «заменить»: экспорт сериализует `{ projects, projectOrder, activeProjectId, projectCounter, crossProjectLinks, canvas }` (только так можно унести кросс-проектные связи — они не принадлежат ни одному проекту по отдельности); импорт диспатчит `LOAD_GLOBAL_STATE` через `rawDispatch` (НЕ через локально-обёрнутый `dispatch` этого компонента — тот на действиях, отличных от списка исключений, заворачивает всё в `FOR_PROJECT`, адресуя внутреннему одно-проектному `reducer`, у которого просто нет такого case, — экшен обязан попасть в `multiReducer` напрямую). `sanitizeLoadedProjects(rawProjects, rawProjectOrder)` — санитизация, общая с `getInitialMultiState` (нормализация окон/связей, чистая история): вынесена в `reducer.js`, чтобы оба пути (загрузка из `localStorage` при старте и импорт файла) не расходились. После загрузки — `reconcilePendingGateways` (на случай, если в самом файле уже лежали обе половины разорванной связи). UI спрашивает подтверждение ДО диспатча (`window.confirm`, деструктивная операция).

**Неактивные проекты полностью интерактивны (PLAN V6, 2026-08-28).** Узел, слой, порт и связь чужого (неактивного) проекта на общем холсте кликабельны, выделяются, редактируются инлайн и перетаскиваются наравне с активным — ограничение `pointer-events: none` снято. Клик по элементу неактивного проекта мягко (по `onClick`, не по `mousedown`, чтобы не срывать жест) синхронизирует активный проект для верхних панелей.

**Как компоненты холста получают состояние своего проекта (`ProjectContext`).** Компоненты холста (`Node.js`, `Port.js`, `Frame.js`, `Link.js`, `Lane.js`, `LaneWindow.js`) НЕ читают активный проект напрямую — у них есть проектно-адресный слой поверх `Store.js`:
* `ProjectContext` — React-контекст с `projectId` того окна на холсте, в котором рендерится компонент (`Canvas.js` оборачивает каждое окно и Drag-оверлей в `<ProjectContext.Provider value={projectId}>`).
* `getProjectFlatView(projectId)` — плоский вид ЛЮБОГО проекта (не только активного), кэшированный по стабильной ссылке на `state.projects[projectId]`.
* `useProjectSelector(selector)` / `useProjectDispatch()` — берут `projectId` из `ProjectContext`; запись в проект, отличный от активного, автоматически заворачивается в `FOR_PROJECT`.
* `useProjectStore()` (2026-08-29) — аналог `useStore()` (весь плоский вид проекта + `dispatch`, без селектора), но привязанный к `ProjectContext`. Используется в `AIAgentNodeContent.js`: узел-ассистент рендерится внутри `NodeView` конкретного проекта и обязан читать/писать СВОЙ проект, даже если активен другой. До этой правки узел использовал `useStore()` и в неактивном проекте молча читал и менял активный проект — баг того же семейства, что чинили для окон уровней.

Панели (`Toolbar.js`, `Library.js`, `ContextActionBar.js`) на проектно-адресный слой НЕ переведены — используют обычные `useStore()`/`useSelector()`/`useDispatch()` и осознанно работают только с активным проектом.

#### Кросс-проектные связи (Фаза 6.1) и штекеры разрыва (Фаза 6.2)

| Экшен | Payload | Описание |
|---|---|---|
| `ADD_CROSS_PROJECT_LINK` | `{ id?, sourceProjectId, sourcePortId, targetProjectId, targetPortId, name?, color?, linkStyle? }` | Создать связь между портами ДВУХ РАЗНЫХ проектов. Обрабатывается в `multiReducer` напрямую (не делегируется) |
| `REMOVE_CROSS_PROJECT_LINK` | `id` | Удалить кросс-проектную связь |
| `UPDATE_CROSS_PROJECT_PROXY_PORT` | `{ linkId, windowId, edge, fraction }` | Аналог `UPDATE_PROXY_PORT` для связи из `crossProjectLinks` (пишет в `link.proxyOverrides[windowId]`, а не в `state.links`) |
| `UPDATE_PENDING_GATEWAY_PROXY` | `{ linkId, edge, fraction, skipHistory? }` | Ручное положение прокси НЕПРИМИРЁННОГО штекера (`project.pendingGateways[linkId]`) — проектное поле, участвует в обычном Undo проекта |

**`state.crossProjectLinks`** — глобальное поле (НЕ `PROJECT_FIELD`), форма как у обычной связи + `sourceProjectId`/`targetProjectId`. Живёт ВНЕ истории Undo обоих проектов, как `canvas`/`containerIsolation`: связь трогает ДВА проекта разом, а `past`/`future` — раздельная история КАЖДОГО проекта, единого места для одного атомарного шага здесь нет.

**v14 (Фаза 5): резолвер цели в `Port.js` снова видит порты всех проектов.** Шаг 1 — ближайший порт своего проекта (`HierarchyUtils.getPortWorldPositionV14`); шаг 1.2, если ничего не нашлось — сканирует `state.projectOrder` остальных проектов через `getProjectFlatView(pid)` + тот же `getPortWorldPositionV14` (окна всех проектов уже на общем холсте в одной мировой системе координат, отдельной прокси-геометрии на грани окна не нужно). Порт в чужом проекте диспатчит `ADD_CROSS_PROJECT_LINK` вместо `ADD_LINK`. Абзац ниже описывает СТАРЫЙ (v13) резолвер (`Layer.js`, не вызывается с Фазы 4) — семантика та же, но через мёртвый код.

**Резолвер цели старого `Port.js` (v13, не вызывается с Фазы 4) видел порты всех проектов.** Поиск ближайшего порта под курсором при отпускании связи сначала искал в своём проекте, затем — если ничего не найдено — сканировал `state.projectOrder` остальных проектов через `getProjectFlatView(pid)` (окна всех проектов уже на общем холсте в одной мировой системе координат). Найденный порт в чужом проекте диспатчил `ADD_CROSS_PROJECT_LINK` вместо `ADD_LINK`.

**v14 (Фаза 5): `Canvas.js`'s `CrossProjectLinkLayer` рисует `crossProjectLinks` одной прямой линией в мировых координатах** — та же идея, что у `CrossWindowLinkLayer` (Фаза 4): окна ВСЕХ проектов уже лежат в одном мировом пространстве, так что `HierarchyUtils.getPortWorldPositionV14(portId, getProjectFlatView(pid))` для обеих сторон достаточно, никакой отдельной прокси-геометрии на грани окна (border-dot/`buildWindowProxyGeometry`/`getExternalProxyForLink`) не нужно. Компонент ОДИН на холст (не на проект), вне `ProjectContext.Provider`; связь не рисуется, если хоть один порт сейчас невидим (его дорожка нигде не открыта, §2.3 LANES_MODEL.md) — не баг, а прямое следствие инварианта «не открыто → не отображается». Абзац ниже описывает СТАРЫЙ (v13) слой прокси-геометрии, полностью удалённый живым кодом с Фазы 4 (`LevelWindow.js`/старый `Canvas.js` не загружаются).

**Удаление проекта демоутит связь, а не теряет её.** `applyRemoveProject` для каждой `crossProjectLinks`-записи, задевающей удаляемый проект: если проект на другой стороне ещё жив — запись переносится в его `pendingGateways[linkId]` (снимок направления, удалённого порта/проекта по имени, грани/доли прокси на момент разрыва) вместо молчаливого удаления; если гибнут обе стороны — запись пропадает совсем. `pendingGateways` — `PROJECT_FIELD`.

**`externalGateway`: локальный экспорт и автовосстановление (Фаза 6.2).** `handleExportProject` (`ContextActionBar.js`) добавляет в файл `externalGateways` — по одной записи на каждую `crossProjectLinks`, задевающую экспортируемый проект, с тем же `linkId`, что был у живой связи. При импорте («добавить как новый проект», `ADD_PROJECT_FROM_FILE`) эти записи копируются в `pendingGateways` нового проекта как есть — id портов внутри одного файла коллизий не создают. `reconcilePendingGateways(m)` (вызывается после `ADD_PROJECT_FROM_FILE` и после демоушена в `applyRemoveProject`) группирует `pendingGateways` ВСЕХ проектов по `linkId`; там, где нашлась ровно одна пара с противоположными `direction` ('out'+'in') — синтезирует живую `crossProjectLinks`-запись и убирает оба штекера. **Сверка идёт только по `linkId`, БЕЗ проверки совпадения `remoteProjectId`** — при повторном импорте контрагент получает СВЕЖИЙ `projectId` (`newProjectId()`), а уцелевшая сторона продолжает помнить СТАРЫЙ, уже удалённый; именно нестабильность `projectId` между экспортом/импортом и есть причина, почему опорный идентификатор — `linkId` связи, а не id проекта на другом конце. Непримирённый штекер рисуется как маркер разрыва на рамке окна — `HierarchyUtils.getPendingGatewayProxiesForWindow(windowId, projectId, multiState)` (грань/доля берутся из самой записи, НЕ пересчитываются — сравнивать не с чем, второе окно не загружено), только внутренний отрезок, без магистрали (`CrossProjectLinkLayer` штекеры не рисует). Тултип у штекера — `«{remotePortName}» в проекте «{remoteProjectName}» (сейчас не загружен)`.

#### Кросс-проектный перенос сущностей (Фаза 6.3)

Перенос узла/слоя (и его ветки) между проектами живым Drag&Drop — не поповером и не отдельным экшеном создания, а тем же самым жестом, что уже переносит сущности внутри проекта: неактивные проекты и так полностью рендерятся на общем холсте в единой мировой системе координат, так что задача сводится к расширению резолвера цели на сканирование всех проектов, а не к новому UI.

- **`HierarchyUtils.getDropTarget(draggedIds, pointerWorld, state, opts, sourceState?)`** — 5-й параметр (по умолчанию `null` → тот же `state`, поведение не меняется): `dragged`/`dragRects`/`draggedLevels`/`unsupportedMixedLayer` считаются из `sourceState` (проект перетаскиваемых сущностей), кандидаты цели (`nodes`/`layers`/`levelWindows`) — по-прежнему из `state` (проект под курсором). «Своё окно» (`isMove`) определяется буквальным совпадением id окна (глобально уникален между проектами), а не номера уровня — у кросс-проектного дропа номер уровня цели может случайно совпасть с номером уровня источника, оставаясь другим окном.
- **`HierarchyUtils.canReparentTo(entityId, targetParentId, nodes, layers, levelWindows, entityDicts?)`** — 6-й параметр `{nodes, layers}` сущности, если отличаются от целевых словарей: существование `entityId` проверяется в НИХ, цикл — пропускается (геометрически невозможен между двумя разными деревьями).
- **`HierarchyUtils.computeDropPositions(ids, win, state, sourceState?)`** — аналогично: мировые координаты переносимых берутся из `sourceState`, камера/зум целевого окна — из `state`.
- **`HierarchyUtils.getDropTargetAcrossProjects(draggedIds, pointerWorld, multiState, sourceProjectId, opts)`** — обёртка над `getDropTarget`: для каждого `pid` из `multiState.projectOrder` (кроме скрытых изоляцией) вызывает его с `getProjectFlatView(pid)` в роли `state` и `getProjectFlatView(sourceProjectId)` в роли `sourceState`; лучший результат — по приоритету node > layer > window, валидный побеждает невалидный. Возвращает тот же формат, что `getDropTarget`, плюс `projectId` найденной цели.
- **⚠️ v14 (Фаза 4): весь этот подраздел (кроме `applyCrossProjectReparent` ниже) описывает СТАРЫЙ (v13) механизм, который физически остаётся в коде, но с конца Фазы 4 не имеет живого пути активации.** Новый `Node.js` использует однопроектный `HierarchyUtils.resolveDropTarget` (не `getDropTargetAcrossProjects`) и никогда не передаёт `sourceProjectId`/`targetProjectId` в `REPARENT_ENTITY` — кросс-проектный перенос карточки живым Drag&Drop-жестом по-прежнему недостижим из UI (только программно/тестами), пока `resolveDropTarget` не получит v14-вариант, сканирующий `multiState.projectOrder` (задача, аналогичная старому `getDropTargetAcrossProjects`, но не сделанная в Фазе 5).
- **Жест в старом `Node.js`/`Layer.js` (v13, не вызывается с Фазы 4):** `computeTarget` вызывал `getDropTargetAcrossProjects` вместо `getDropTarget` (проект перетаскиваемой сущности уже активен — форсирован на mousedown). На `mouseup`, если `target.projectId` отличался от исходного проекта — `REPARENT_ENTITY` получал дополнительные `sourceProjectId`/`targetProjectId`; окно-цель резолвилось из `getProjectFlatView(target.projectId)`, а не из своего `st`. Deep/Shallow (Alt) работали одинаково в обоих случаях — Shallow переприкреплял детей к деду ВНУТРИ исходного проекта до того, как верхняя сущность уезжала.
- **`applyCrossProjectReparent(m, payload)` в `multiReducer` — переписана на v14 в Фазе 5 (§5 плана), барьер `formatVersion >= FORMAT_VERSION_V14` снят.** Перехватывает `REPARENT_ENTITY`, когда `sourceProjectId`/`targetProjectId` заданы и различны (иначе — как раньше, `delegateToActiveProject`). Ветвится по `m.formatVersion`: `< 14` — старая v13-логика (`nodes`/`layers`, `H.isDescendantOf`/`H.canReparentTo`, `normalizeLevelWindows`) остаётся нетронутой ради фикстур тестов, собранных `wrapFlatToMulti` НАПРЯМУЮ (без `migrateToV13`/`migrateToV14`, см. `projects.test.js`); `>= 14` (реальный живой путь — все проекты после `getInitialMultiState` уже v14) — только узлы (`H.isDescendantOfV14`/`H.canReparentToV14(eid, targetParentId, targetView.nodes, { nodes: sourceView.nodes })`, целевая сущность-родитель проверяется в СВОЁМ проекте, существование переносимой — в ЧУЖОМ), окно целевого узла без открытой дорожки открывается автоматически через `applyOpenLaneV14` (§0.4.3), как и в однопроектном `REPARENT_ENTITY`. В обеих ветках: собирает ветку (сущность + структурные потомки по `parentId`, их порты, полностью внутренние связи) из исходного проекта, при Shallow — сперва переприкрепляет прямых детей к деду; вставляет в целевой проект (`findFreePosition` на корне `targetParentId`, либо явная позиция под курсором); `crossProjectLinks`/`pendingGateways`, чей порт уехал вместе с веткой, переписываются на новый `projectId` общим хелпером `relocateCrossProjectGateways`. **Не участвует в Undo ни одного из проектов** (как и создание/удаление `crossProjectLinks`).
- **Подсветка цели по `projectId`.** `dragGesture` — глобальное поле, видно во всех проектах разом; `Node.js`/`Layer.js`/`LevelWindow.js` при матчинге `dragGesture.target` теперь также сверяют `target.projectId` с собственным — иначе гипотетическое совпадение id между двумя проектами подсветило бы не тот узел.
- **`buildTransferConfirmText(ids, target, state, mode, targetState?)`** — 5-й параметр для текста подтверждения, когда цель в другом проекте (иначе имена/уровень резолвились бы из чужих словарей); добавляет «(проект «X»)» к описанию цели.

#### Выделение контейнеров и их изоляция

| Экшен | Payload | Описание |
|---|---|---|
| `TOGGLE_CONTAINER_ISOLATION` | `{ kind: 'project'\|'window', id }` | Включить/снять изоляцию контейнера на общем холсте |
| `SET_CONTAINER_ISOLATION` | `{ projectIds, windowIds }` | Задать набор изолированного целиком |
| `CLEAR_CONTAINER_ISOLATION` | — | Снять изоляцию контейнеров |

**Адресация в `selectedIds`.** Контейнеры адресуются префиксами: `project:<projectId>` и `window:<windowId>` (по стабильному id окна — номер уровня не уникален между проектами). Старые формы `'project'` и `level-window-<N>` продолжают распознаваться и означают контейнеры активного проекта.

**Классы выделения взаимоисключающи.** В `selectedIds` лежат либо только контейнеры, либо только сущности графа: добавление элемента другого класса ЗАМЕНЯЕТ выделение. Благодаря этому `DELETE_SELECTED` всегда однозначен — он либо удаляет контейнеры (проекты целиком, окна — `window:<id>`), либо сущности (узлы каскадом, рамки).

#### Пакетная история

| Экшен | Payload | Описание |
|---|---|---|
| `BEGIN_HISTORY_BATCH` | `{ logMessage? }` | Открыть пакет: последующие экшены НЕ пишут свои шаги в историю |
| `COMMIT_HISTORY` | `{ snapshot?, logMessage? }` | Закрыть пакет одним шагом Undo |
| `CANCEL_HISTORY_BATCH` | — | Закрыть пакет без записи (аварийный выход) |

Серия экшенов от ИИ применяется именно так: весь ответ ассистента — **один** шаг Undo. Без этого батч из 30+ команд вымывал лимит истории в 20 шагов, и отменить работу ассистента было невозможно.

---

### 5. Пример сгенерированного графа (JSON Protocol)

```json
[
  {
    "type": "ADD_NODE",
    "payload": {
      "id": "node-canvas-view",
      "name": "Canvas Viewport",
      "content": "Интерактивная область холста",
      "color": "#0f172a",
      "position": { "x": 30, "y": 80 },
      "size": { "w": 250, "h": 120 },
      "parentId": "root",
      "shape": "rectangle"
    }
  },
  {
    "type": "ADD_NODE",
    "payload": {
      "id": "node-store",
      "name": "Store Provider",
      "content": "Хранилище состояния",
      "color": "#0f172a",
      "position": { "x": 310, "y": 80 },
      "size": { "w": 250, "h": 120 },
      "parentId": "root",
      "shape": "rectangle"
    }
  },
  {
    "type": "ADD_PORT",
    "payload": {
      "id": "port-canvas-out",
      "nodeId": "node-canvas-view",
      "type": "output",
      "edge": "right",
      "position": 0.5,
      "name": "Events Dispatch",
      "color": "#38bdf8"
    }
  },
  {
    "type": "ADD_PORT",
    "payload": {
      "id": "port-store-in",
      "nodeId": "node-store",
      "type": "input",
      "edge": "left",
      "position": 0.5,
      "name": "Actions Handler",
      "color": "#0284c7"
    }
  },
  {
    "type": "ADD_LINK",
    "payload": {
      "id": "link-canvas-to-store",
      "sourcePortId": "port-canvas-out",
      "targetPortId": "port-store-in",
      "name": "Redux Dispatch",
      "linkStyle": "orthogonal",
      "color": "#38bdf8"
    }
  },
  {
    "type": "ADD_FRAME",
    "payload": {
      "members": ["node-canvas-view", "node-store"],
      "name": "1. UI Layer",
      "content": "Пользовательский интерфейс и представление",
      "color": "#0284c7"
    }
  }
]
```

### 6. Ключевые инварианты при генерации портов и связей:

1. **Порты ОБЯЗАТЕЛЬНЫ:** Нейросеть должна самостоятельно генерировать порты (`ADD_PORT`) для узлов, задавая им подписи `name`, тип `input`/`output`, грань `edge` (`left`, `right`, `top`, `bottom`) и HEX-цвет.
2. **Связи соединяют ТОЛЬКО Порты:** Экшен `ADD_LINK` принимает в `sourcePortId` и `targetPortId` **строго ID ранее созданных портов**, а не ID узлов.
3. **Авто-выравнивание элементов на слое:** Координаты нод на слое следует разносить по сетке (`{x: 30, y: 80}`, `{x: 310, y: 80}`). При добавлении узлов приложение автоматически применяет к слою выравнивание `GeometryUtils.getSmartPlacement`.

---

## 🧪 Обязательное тестирование после изменений

После любых изменений в логике, редюсере, геометрии или компонентах **ИИ-ассистент обязан выполнить две команды проверки**:

1. **Юнит-тесты Node.js:**
   ```bash
   cmd /c "node --test app/tests/*.test.js"
   ```
   *Все тесты должны проходить со статусом PASS (0 ошибок). Точное число намеренно не дублируется в документации — единственный источник правды это вывод самого прогона.*

2. **Статическая проверка типов TypeScript:**
   ```bash
   cmd /c "npx -y -p typescript tsc -p app/jsconfig.json"
   ```
   *Команда должна завершаться с нулевым кодом ошибки.*

---

## 📌 Памятка для ИИ по стилю внесения правок

* **Не нарушать работу Zero-Build:** Не использовать `require()` или `import` в браузерных React-компонентах.
* **Редьюсер не вызывает сам себя:** рекурсивный вызов `reducer(...)` внутри `const reducer = ...` ломает загрузку через babel-standalone (top-level `const` перестаёт быть виден другим скриптам). Общую логику выносить в top-level функции (пример: `applyRemoveLevelWindow`).
* **Кэш скриптов:** после правок компонентов поднимать метку версии `?v=...` у всех script-тегов в `app/index.html` — иначе браузер пользователя исполняет старый код.
* **Видимость окон/проектов** проверять через `HierarchyUtils.isWindowVisible`/`isProjectVisible(id, containerIsolation)` — единая точка истины для изоляции контейнеров. Ветвление/«глаз» на уровне отдельной ветки (v13 `isEntityVisible`) в v14 не существует.
* **Цель создания элементов** («+» в `Toolbar.js`) определять через `HierarchyUtils.getAddContextV14(state)` (`activeLaneId`, либо дорожка единственного выделенного узла, либо `'root'`); при `ok:false` действие запрещено (множественное выделение).
* **Подписки на DOM-узлы условного рендера** (например, вьюпорт окна уровня, размонтируемый при сворачивании) вешать через callback-ref, а не `useEffect` с deps — слушатель должен следовать за жизненным циклом узла.
* **Использовать GitHub Flavored Markdown links:** При ответах пользователю создавать кликабельные ссылки на файлы вида `[basename](file:///absolute/path/to/file)`.
* **Сохранять docstrings и комментарии:** Не удалять существующие комментарии, описывающие бизнес-логику и математику выталкивания.
* **Использовать атомарные edits:** При правках файлов использовать точечную замену `replace_file_content` или `multi_replace_file_content`.
